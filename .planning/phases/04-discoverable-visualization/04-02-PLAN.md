---
phase: 04-discoverable-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - FluidDynamicsMetaliOS/RenderViewController.swift
autonomous: true
requirements:
  - CTRL-01
must_haves:
  truths:
    - "D-01, D-02, D-03, D-04: The iOS HUD stays visible over the full-bleed MTKView at the lower trailing safe-area edge, with native material and a semantic opaque Reduce Transparency fallback."
    - "D-05: The four named field buttons expose the selected field and a separate Pause/Resume button follows renderer.state.userPaused, including after returning to the app."
    - "D-06: Button taps, one-finger pause and two-finger field-cycle shortcuts use the same Renderer state; selecting a field while paused immediately presents it without advancing the solver."
    - "D-07: A HUD touch never creates dye/force or triggers a canvas tap shortcut, even with a second finger outside the HUD; touches elsewhere continue to stir. Controls fit iPhone/iPad safe areas in portrait, landscape and multitasking."
  artifacts:
    - FluidDynamicsMetaliOS/RenderViewController.swift
  key_links:
    - "iOS field actions call Renderer.selectField(_:); `changeSource()` and `doubleTap()` refresh from renderer.state; `didBecomeActive()` refreshes after renderer.becomeActive()."
    - "The MTKView remains root and receives off-HUD touches; its three existing UITapGestureRecognizers reject each touch that begins inside the HUD before recognizing canvas gestures."
---

<objective>
As an iPhone or iPad user, I want to select any named fluid view and pause or resume from an always-visible control group so I can explore the simulation while still stirring the canvas.

Purpose: Bring the same direct renderer selection to iOS while retaining touch shortcuts and preventing the new controls from generating fluid input.
Output: An adaptive UIKit HUD over the existing Metal canvas with safe, per-touch event routing.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-discoverable-visualization/04-CONTEXT.md
@.planning/phases/04-discoverable-visualization/04-RESEARCH.md
@.planning/phases/04-discoverable-visualization/04-VALIDATION.md
@.planning/phases/04-discoverable-visualization/04-UI-SPEC.md
@.planning/phases/04-discoverable-visualization/04-PATTERNS.md
@.planning/phases/04-discoverable-visualization/04-01-SUMMARY.md
@.planning/phases/02-modern-ios-parity/02-CONTEXT.md
@.planning/phases/03-reliable-simulation-state/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select and pause fields from a touch-safe iOS HUD</name>
  <files>FluidDynamicsMetaliOS/RenderViewController.swift</files>
  <read_first>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOS/Base.lproj/Main.storyboard, Shared/SimulationState.swift, Shared/Renderer.swift, FluidDynamicsMetalOSX/RenderViewController.swift, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md, .planning/phases/04-discoverable-visualization/04-CONTEXT.md, .planning/phases/04-discoverable-visualization/04-RESEARCH.md, .planning/phases/03-reliable-simulation-state/03-CONTEXT.md</read_first>
  <action>In the existing iOS controller add a compact `UIVisualEffectView` HUD as a subview of the storyboard root `MTKView`, initially constrained to the trailing and bottom safe-area edges with 16 pt insets and with its own bounded content width. Place a `View` caption, native UIKit buttons `Density`, `Pressure`, `Velocity`, `Vorticity` in enum order, and a separate `Pause`/`Resume` action inside the actual compact surface (no full-screen touch-intercepting wrapper). Field actions call `renderer.selectField(_:)`; pause calls `renderer.togglePause()` after `cancelPendingHolds()` and `clearTouches()` so held input cannot reappear. Add `refreshControls()` reading `renderer.state.field` and `renderer.state.userPaused` to update exactly one selected state, non-color outline/checkmark, accessibility choice values `Selected`/`Not selected`, and pause accessibility label `Pause simulation`/`Resume simulation`; call it after HUD actions, `changeSource()`, `doubleTap()` and `didBecomeActive()` after `renderer.becomeActive()`. Preserve the one-finger/two-finger recognizer precedence and the existing canvas touch bookkeeping. Set a `UIGestureRecognizerDelegate` (or equivalent per-touch filtering) on all three root tap recognizers; reject a touch whose starting `touch.view` is the HUD or a descendant or whose initial location is in actual HUD bounds, without rejecting a simultaneous outside touch. Ensure `touchesBegan` never enters a HUD-origin touch into `touchOrder`, `positions`, `pendingHolds` or `renderer.enqueueTap`; an outside touch still reaches those paths. `singleTap(_:)` must not enqueue dye for HUD-origin taps. Use native button hit testing and allow drags next to the HUD; retain full-size root MTKView, existing solver and prior resize/lifecycle handling.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - iOS Debug simulator build exits 0 with a root MTKView and a compact lower-trailing HUD; controls route to `Renderer.selectField(_:)` and `Renderer.togglePause()` and reflect `renderer.state` after legacy gestures and app return.
    - All three canvas tap recognizers reject HUD-origin touches individually; HUD-origin touches do not enter `touchOrder` or `pendingHolds`, and single-tap dye cannot be enqueued from inside HUD bounds. Outside touches and existing two-finger shortcut precedence remain intact.
    - The HUD contains four full names, an action labeled Pause/Resume, `Display field` group labeling and non-color active selection; no renderer/shader or full-screen gesture-wrapper replacement was introduced.
  </acceptance_criteria>
  <done>An iOS user can select and inspect a named field and pause/resume using controls without adding accidental density or losing canvas stirring.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt the iOS HUD to safe areas, text sizes and transparency</name>
  <files>FluidDynamicsMetaliOS/RenderViewController.swift</files>
  <read_first>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOS/Base.lproj/Main.storyboard, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md, .planning/phases/04-discoverable-visualization/04-CONTEXT.md, .planning/phases/04-discoverable-visualization/04-VALIDATION.md, FluidDynamicsMetalOSX/RenderViewController.swift</read_first>
  <action>Finish iOS layout to the approved native UI spec: system font metrics (`.caption1` for `View`, `.subheadline` for field/pause labels), 8 pt field gaps, 16 pt HUD padding and safe-area insets, 12 pt corner radius, and at least 44 pt button hit heights. Prefer enum-order two-by-two choices and a full-width pause row; if measured text and available safe-area width/height allow, use four field buttons in one row in wide/short layouts; omit only the visible `View` caption in compact height (keep accessibility group `Display field`). If neither fits, use a single column and intrinsic-height buttons; constrain HUD width to safe-area width minus 32 pt and height to safe-area height minus 32 pt, without truncation/horizontal scrolling at standard text sizes. At extreme accessibility sizes, allow bounded vertical scrolling *within the HUD*, keep `Pause`/`Resume` and `View: {active field}` summary visible and keep all four choices reachable. Support Dynamic Type and right-to-left trailing alignment, including trait/safe-area updates on rotation and iPad multitasking. Use semantic label colors and system accent with persistent outline/checkmark for selection; prefer native `UIVisualEffectView` material but use `.systemBackground` when `UIAccessibility.isReduceTransparencyEnabled` and respond to Reduce Transparency changes. Maintain a compact hit area after each layout change so gestures outside HUD continue to reach the canvas. Avoid shaders, live tuning, resolution and reset controls.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - iOS simulator Debug build exits 0; layout constraints remain within the safe area and limit HUD width/height with 16 pt outer insets and minimum 44 pt button hit heights.
    - Source preserves full names and a visible pause action when compact; adaptive layout uses available safe-area dimensions and intrinsic text metrics, with bounded internal scrolling and persistent active-field summary at extreme text sizes.
    - Material can switch to semantic opaque `.systemBackground` on Reduce Transparency; selection has a non-color cue and foreground uses semantic colors.
  </acceptance_criteria>
  <done>The iOS visual slice remains legible and tappable across compact iPhone, iPad and accessibility layouts without obscuring the main fluid canvas.</done>
</task>

</tasks>

<threat_model>
No network, external data or persistence is added. The integrity risk is HUD-origin touches being handled by ancestor canvas recognizers or held-contact bookkeeping and producing unintended dye/force; per-touch recognizer rejection and touch bookkeeping exclusion address it. High-severity external threats: none identified.
</threat_model>

<verification>
- [ ] iOS simulator Debug build compiles the controller using the shared direct-selection API from Plan 01.
- [ ] Recognizer/Touch routing is per touch; the HUD remains operable while another finger stirs elsewhere on canvas.
- [ ] Live iPhone/iPad layouts, gesture behavior, transparency and foreground sync are observed at Plan 03's checkpoint rather than inferred from compilation.
</verification>

<success_criteria>
- iPhone/iPad users can directly select and identify four named fields and use Pause/Resume over a central interactive fluid canvas; old one-/two-finger double-tap shortcuts remain usable (CTRL-01, D-01–D-07).
</success_criteria>

<output>
Create `.planning/phases/04-discoverable-visualization/04-02-SUMMARY.md` with the iOS build result and explicit outstanding live input/layout checks.
</output>
