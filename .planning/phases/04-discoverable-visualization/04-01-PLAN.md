---
phase: 04-discoverable-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/SimulationState.swift
  - Shared/Renderer.swift
  - FluidDynamicsMetalStateTests/SimulationStateTests.swift
  - FluidDynamicsMetalOSX/RenderViewController.swift
autonomous: true
requirements:
  - CTRL-01
must_haves:
  truths:
    - "D-01, D-02, D-03, D-04: A compact, always-visible native-material Mac HUD stays in the upper trailing corner over the full-size interactive MTKView and has an opaque Reduce Transparency fallback."
    - "D-05: Density, Pressure, Velocity and Vorticity are all named in the HUD; exactly one is visibly selected and a separate action says Pause or Resume according to renderer.state."
    - "D-06: Directly selecting a field while user-paused presents its stored slab immediately without solver advancement; Mac Space/S still work and update HUD state, and Space on a focused HUD control does not toggle twice."
    - "D-07: Clicking the HUD activates only its buttons while pointer drags elsewhere still stir fluid; narrow Mac windows do not clip labels."
  artifacts:
    - Shared/SimulationState.swift
    - Shared/Renderer.swift
    - FluidDynamicsMetalStateTests/SimulationStateTests.swift
    - FluidDynamicsMetalOSX/RenderViewController.swift
  key_links:
    - "Mac HUD actions and Space/S both call Renderer.selectField(_:), Renderer.togglePause() or Renderer.nextSlab(); refresh reads Renderer.state rather than maintaining a separate field/pause cache."
    - "Renderer.selectField(_:) follows nextSlab()'s paused-and-active draw guard, and Renderer.draw(in:) already has a solver-free presentation branch."
---

<objective>
As a Mac user, I want to choose a named fluid view and pause or resume from visible controls so I can inspect the simulation without learning shortcuts.

Purpose: Deliver the first end-to-end, on-screen selection path and shared state contract; retain the full-canvas fluid and the existing Space/S controls.
Output: Direct shared selection with executable state tests and a responsive, native Mac floating HUD.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-discoverable-visualization/04-CONTEXT.md
@.planning/phases/04-discoverable-visualization/04-RESEARCH.md
@.planning/phases/04-discoverable-visualization/04-VALIDATION.md
@.planning/phases/04-discoverable-visualization/04-UI-SPEC.md
@.planning/phases/04-discoverable-visualization/04-PATTERNS.md
@.planning/phases/03-reliable-simulation-state/03-CONTEXT.md
@.planning/phases/03-reliable-simulation-state/03-SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select a named field directly on the Mac canvas</name>
  <files>Shared/SimulationState.swift, Shared/Renderer.swift, FluidDynamicsMetalStateTests/SimulationStateTests.swift, FluidDynamicsMetalOSX/RenderViewController.swift</files>
  <read_first>Shared/SimulationState.swift, Shared/Renderer.swift, FluidDynamicsMetalStateTests/SimulationStateTests.swift, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetalOSX/Base.lproj/Main.storyboard, .planning/phases/04-discoverable-visualization/04-CONTEXT.md, .planning/phases/04-discoverable-visualization/04-RESEARCH.md, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md, .planning/phases/04-discoverable-visualization/04-VALIDATION.md</read_first>
  <action>First extend the production XCTest suite with a test that selects `.vorticity` directly from default `.density`, selects `.pressure` while user-paused, reselects `.pressure` idempotently, and verifies field and user pause persist through inactive/active transitions; keep the existing `nextField()` wraparound assertion. Run the test once to see it fail before implementation. Add `SimulationState.selectField(_ field: DisplayField)` assigning the requested field without changing `userPaused` or `inactive`. Add `Renderer.selectField(_ field: DisplayField)` that updates state and calls `metalView?.draw()` only when `!state.shouldAdvance && !state.inactive`, matching `nextSlab()`; preserve `draw(in:)`'s solver-free paused branch and the `nextSlab()` field order. In the existing Mac controller, create a small `NSVisualEffectView` HUD as a subview of the root `MTKView` and a first usable group with four labeled native field buttons (`Density`, `Pressure`, `Velocity`, `Vorticity`) and a separate `Pause`/`Resume` button. Pin it upper trailing with 16 pt inset, never shrink the MTKView, route named buttons to `renderer.selectField(_:)` and pause to `renderer.togglePause()`. Add one `refreshControls()` reading `renderer.state.field` and `renderer.state.userPaused`, called after button actions and from existing Space/S handlers; selected field must have a persistent non-color cue and accessibility value `Selected`/`Not selected`. Keep existing drag handling; do not edit Metal shaders, solver constants or storyboards. This task is the working Mac user path; refine adaptive visual treatment in Task 2.</action>
  <verify><automated>xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO</automated></verify>
  <acceptance_criteria>
    - `SimulationStateTests.swift` includes direct out-of-order selection, repeat selection, paused selection and inactivity/return assertions against production `SimulationState`; the Mac XCTest command exits 0 with those cases executed.
    - `Renderer.selectField(_:)` uses the same `!state.shouldAdvance && !state.inactive` draw guard as `nextSlab()`; `draw(in:)`'s paused branch still calls only `render`, `present` and `commit`, not advection or other solver passes.
    - The Mac controller attaches five named buttons to the full-size MTKView; they update the renderer directly and `refreshControls()` reflects the same state as Space/S.
  </acceptance_criteria>
  <done>The Mac app can directly choose and identify each field, pause and resume via an always-visible floating control group; shared state transitions have runnable test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Mac HUD usable at compact sizes and during shortcut input</name>
  <files>FluidDynamicsMetalOSX/RenderViewController.swift</files>
  <read_first>FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetalOSX/Base.lproj/Main.storyboard, Shared/SimulationState.swift, Shared/Renderer.swift, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md, .planning/phases/04-discoverable-visualization/04-CONTEXT.md, .planning/phases/04-discoverable-visualization/04-VALIDATION.md</read_first>
  <action>Finish the native Mac HUD to match the approved UI contract: `View` caption and enum-order two-by-two field choices above a full-width Pause/Resume action; 16 pt inner/outer insets, 8 pt control spacing, 12 pt corner radius, minimum 28 pt button heights and intrinsic text widths. Respect trailing safe-area/window edges, right-to-left layout, semantic label/secondaryLabel colors and system accent; represent selection with accent plus outline/checkmark and semibold label, never color alone. Use a native material while transparency is allowed and `NSColor.windowBackgroundColor` as the opaque surface when system Reduce Transparency is on, including changes while the app runs. When the view is attached to its window, set a 320×240 pt minimum content size at standard text sizes and raise it for larger text if necessary; keep HUD width/height within the content-area size minus 32 pt. Use measured/intrinsic widths to switch to one column at large text sizes rather than clipping/truncating names; at extreme text sizes permit bounded vertical scrolling *inside the HUD* while pinning Pause/Resume and a `View: {active field}` summary above the field choices. Keep native focus rings and `Display field` accessibility group label; expose Pause/Resume as `Pause simulation`/`Resume simulation`. Change the existing local `NSEvent` Space handler so a keyDown for Space on a focused HUD button is handled by that native button only, while Space elsewhere toggles once and S continues to cycle once; always refresh from renderer state. Ensure button mouseDown cannot set `mouseHeld` or deposit dye and a click or drag directly adjacent to the compact HUD still reaches the canvas.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - Mac Debug build exits 0; controller retains the storyboard root MTKView and calls the existing renderer input APIs for canvas drags.
    - HUD source contains `Density`, `Pressure`, `Velocity`, `Vorticity`, `View`, `Pause`, `Resume`, `Display field`, `Selected` and `Not selected`; selected control has a non-color indicator and Pause/Resume reflects `renderer.state.userPaused`.
    - Mac material uses a semantic opaque Reduce Transparency fallback; constraints bound the floating HUD inside a 320×240 pt minimum content area with complete visible labels at standard text size.
    - Space on a focused HUD button cannot invoke both button activation and the window key monitor's pause handler; Space/S still work when canvas has focus.
  </acceptance_criteria>
  <done>The Mac visual slice is usable over the canvas, with responsive layout, legible state and no shortcut/input collisions.</done>
</task>

</tasks>

<threat_model>
Local UI input and in-memory Metal textures only; no network or persistence. The main integrity risk is accidental fluid input or double pause from the overlay; compact hit-testing, focused-control key routing and retained renderer state address it. High-severity external threats: none identified.
</threat_model>

<verification>
- [ ] The Mac scheme runs new and existing XCTest state transitions, including out-of-order direct selection and pause preservation.
- [ ] Mac Debug build compiles the native HUD without changing storyboard ownership or Metal solver code.
- [ ] Live Mac appearance, hit-testing and focused-Space behavior are observed at the Phase 4 checkpoint (Plan 03), not inferred from a build.
</verification>

<success_criteria>
- A Mac user can see four named views, select one directly and recognize the active field, pause/resume on screen, continue stirring elsewhere on the full canvas, and retain Space/S shortcuts (CTRL-01, D-01–D-07).
</success_criteria>

<output>
Create `.planning/phases/04-discoverable-visualization/04-01-SUMMARY.md` with test count, Mac build result and outstanding live UI observations.
</output>
