---
phase: 05-live-fluid-tuning
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - FluidDynamicsMetaliOS/RenderViewController.swift
  - FluidDynamicsMetaliOSUITests/HUDUITests.swift
autonomous: true
requirements:
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "D-01, D-02: iPhone and iPad have Show/Hide Tuning in the existing lower-trailing field/pause HUD; expansion persists through canvas interaction, field choice, pause, rotation and foreground return until explicitly hidden."
    - "D-03: Every field, pause and tuning control is reachable by scrolling inside the HUD in small safe areas or with larger Dynamic Type, without covering the whole canvas or nesting active scroll regions."
    - "D-04, D-05: iOS drag and single-tap dye use the same independent Force/Dye renderer values as Mac, while outside-HUD canvas double-tap shortcuts still function."
    - "D-06, D-07, D-08, D-09, D-10: Swirl/Fade modify running fluid on the next frames; all four slider labels, percentages, detent and default behavior match the approved shared mapping."
  artifacts:
    - FluidDynamicsMetaliOS/RenderViewController.swift
    - FluidDynamicsMetaliOSUITests/HUDUITests.swift
  key_links:
    - "Each UISlider sends its continuous 0...1 position to Renderer through the same shared mapping/API as the Mac HUD; refresh reads renderer.state.tuning and does not keep an independent solver cache."
    - "isHUDTouch(_:) excludes only touches originating in actual HUD bounds from raw touches and all canvas recognizers; iOS contact batching and single-tap enqueue remain in Renderer."
---

<objective>
As an iPhone or iPad user, I can tune the same four aspects of the live fluid as on Mac using native sliders within the existing HUD, without losing touch interaction elsewhere.

Purpose: Extend the already-working Mac/shared Metal path to a real iOS user path and preserve the Phase 4 touch and compact-layout contract.
Output: An adaptive iOS Tuning disclosure and focused iOS HUD regression tests.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-fluid-tuning/05-CONTEXT.md
@.planning/phases/05-live-fluid-tuning/05-RESEARCH.md
@.planning/phases/05-live-fluid-tuning/05-UI-SPEC.md
@.planning/phases/05-live-fluid-tuning/05-VALIDATION.md
@.planning/phases/05-live-fluid-tuning/05-PATTERNS.md
@.planning/phases/04-discoverable-visualization/04-UI-SPEC.md
@.planning/phases/02-modern-ios-parity/02-CONTEXT.md
@.planning/phases/05-live-fluid-tuning/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the working iOS tuning path on the existing HUD</name>
  <files>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOSUITests/HUDUITests.swift</files>
  <read_first>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOSUITests/HUDUITests.swift, Shared/SimulationState.swift, Shared/Renderer.swift, FluidDynamicsMetalOSX/RenderViewController.swift, .planning/phases/05-live-fluid-tuning/05-UI-SPEC.md, .planning/phases/05-live-fluid-tuning/05-CONTEXT.md, .planning/phases/05-live-fluid-tuning/05-VALIDATION.md, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md</read_first>
  <action>First extend the iOS `HUDUITests.swift` to assert a fresh app shows `Show Tuning` with no visible tuning sliders, then opening it exposes Force `50%`, Dye `40%`, Swirl `20%`, Fade `17%`; run that focused test once against the old UI to demonstrate failure before implementation. Extend the existing `UIVisualEffectView` content after the Pause/Resume button with a native `Show Tuning` / `Hide Tuning` disclosure and a local `isTuningExpanded` Boolean initialized false; never reset it on field changes, pause, canvas touch, layout or foreground return. Add four continuous `UISlider`s in Force, Dye, Swirl, Fade order, with `UILabel` name and right-aligned live integer-percentage for each and `Higher Fade = faster fade` beneath Fade. Feed each slider's position 0...1 to the existing Renderer tuning API/mapping; initialize and refresh from `renderer.state.tuning`, including while paused. Keep default display values 50%, 40%, 20%, 17%, and make the Fade accessibility hint state its 0% endpoint is the slowest available fade. Give each slider its name as accessibility label, percent as accessibility value, and native adjustable semantics; group with `Fluid tuning`. Preserve the field button labels, `Pause`/`Resume`, lower-trailing safe-area position, native material/opaque Reduce Transparency fallback, `isHUDTouch(_:)`, single tap outside the HUD, one-/two-finger double-tap recognizers, and held-touch rebase on rotation. Do not create a second overlay or change touch-to-impulse scaling. This task must deliver a working iPhone/iPad slider-to-fluid path even before compact-layout refinement.</action>
  <verify><automated>xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO</automated></verify>
  <acceptance_criteria>
    - The focused new iOS HUD test runs and passes on an available iPhone simulator; it finds `Show Tuning`, opens four named continuous sliders and reads Force `50%`, Dye `40%`, Swirl `20%`, Fade `17%`.
    - iOS slider actions call the Renderer tuning API; their positions and labels refresh from `renderer.state.tuning`, including while paused, rather than duplicating solver values in the controller.
    - Neither HUD-origin touch nor HUD double tap calls `enqueueTap`/changes the field or pause through a canvas recognizer; the existing canvas tap and shortcut code paths remain present.
    - `xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build` compiles the shared Swift/Metal contract for iOS.
  </acceptance_criteria>
  <done>On iOS a user can reveal and change all four controls, and new input as well as running fluid uses the same bounded state and Metal code as Mac.</done>
</task>

<task type="auto">
  <name>Task 2: Keep iPhone/iPad controls scroll-reachable and touch-safe</name>
  <files>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOSUITests/HUDUITests.swift</files>
  <read_first>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOSUITests/HUDUITests.swift, Shared/Renderer.swift, Shared/SimulationState.swift, .planning/phases/05-live-fluid-tuning/05-UI-SPEC.md, .planning/phases/05-live-fluid-tuning/05-VALIDATION.md, .planning/phases/02-modern-ios-parity/02-CONTEXT.md, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md</read_first>
  <action>Size the expanded HUD to at most 320 pt wide, at least 192 pt when space allows, and no larger than safe-area width/height minus 32 pt; keep its trailing/bottom 16 pt anchor, full-bleed MTKView and actual HUD-only hit target. Maintain native 44-pt disclosure and slider operable regions, 16-pt inner/outer insets, 8-pt row spacing, untruncated labels/percent text, Dynamic Type label wrapping and semibold disclosure/active view. At ordinary heights pin field choices, Pause/Resume and disclosure above a single internally scrollable tuning section; at short heights or large text switch to one vertical scroll region containing field choices -> Pause/Resume -> disclosure -> four rows, replacing rather than nesting the existing `fieldScroll`; show `View: {active field}` outside that region when fields can scroll away. Preserve Phase 4's 1-/2-column and wide-landscape 4-column field reflow. Use a single native scroll region for touch events in each layout mode; avoid turning the whole canvas into a scroll/hit surface. Extend iOS HUD UI tests for Hide/Reopen retaining an adjusted percentage, field choice and pause with the panel open, rotation/landscape reachability of Fade and Pause, HUD double tap having no shortcut effect, and a double tap outside the expanded bounds toggling pause. Run the existing HUD regression tests as well. If a simulator cannot exercise concurrent real touches, keep that part as a manual observation in Plan 03; do not claim a simulated single tap proves multitouch behavior.</action>
  <verify><automated>xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO</automated></verify>
  <acceptance_criteria>
    - An available iPhone simulator runs new and existing iOS HUD UI tests: four values, persistent expansion, pause/field coexistence, bounded expanded hit area, Hide/Reopen retention and reachable controls in landscape.
    - In compact mode only one vertical scroll area is enabled inside the HUD, all four sliders remain operable, and a visible `View: {active field}` summary survives offscreen field choices.
    - Both app schemes build with no deployment target overrides; the already-built Mac mapping/GPU test commands still pass after iOS-only changes.
    - Touches originating in the actual HUD do not create fluid contacts or activate canvas recognizers; touches immediately outside it continue to use existing iOS gesture and input paths.
  </acceptance_criteria>
  <done>The iOS tuning surface remains usable in portrait, compact landscape and iPad layouts while the existing field/pause and fluid input controls coexist.</done>
</task>

</tasks>

<threat_model>
All input is local and bounded in memory. The main integrity risks are HUD-origin touches leaking into fluid gestures and nested scroll views trapping touch input; retain `isHUDTouch(_:)` for all recognizers/raw touches and enable only one scroll region per constrained layout. High-severity external threats: none identified.
</threat_model>

<verification>
- [ ] Confirm an available iOS 26.4 iPhone 17 Pro using `xcrun simctl list devices available` before using the named destination in the test command; choose another available simulator and adjust the destination if needed.
- [ ] Fresh iOS HUD cases and existing Phase 4 interaction cases execute, plus both schemes build after shared uniform changes.
- [ ] iPad, small-window and physical multitouch behavior are observed separately in Plan 03; unavailable hosts are marked NOT TESTED rather than inferred from simulator automation.
</verification>

<success_criteria>
- Both apps offer the same four live bounded controls in the original floating HUD, with iOS touch and adaptive-layout behavior preserved (CTRL-02, CTRL-03, CTRL-04; D-01–D-10).
</success_criteria>

<output>
Create `.planning/phases/05-live-fluid-tuning/05-02-SUMMARY.md` with iOS test/simulator/build results, default values, touch regression results and pending human observations.
</output>
