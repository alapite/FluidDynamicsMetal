---
phase: 05-live-fluid-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/SimulationState.swift
  - Shared/Renderer.swift
  - Shared/Shaders.metal
  - FluidDynamicsMetalStateTests/SimulationStateTests.swift
  - FluidDynamicsMetalOSX/RenderViewController.swift
  - FluidDynamicsMetalOSXUITests/HUDUITests.swift
  - test_phase05_metal.py
  - test_phase05_metal.swift
autonomous: true
requirements:
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "D-01, D-02, D-08: Mac Show Tuning opens four live Force/Dye/Swirl/Fade sliders inside the existing HUD, remains open across canvas interactions, and Hide Tuning closes it without resetting values."
    - "D-04, D-05: Changing Force or Dye affects only the next running-frame contact, including the next movement in a held stroke; zero Force permits dye and zero Dye permits stirring."
    - "D-06, D-07: Swirl and Fade change already-present fluid on running frames without new input; paused frames do not advance or change their displayed slab."
    - "D-08, D-09, D-10: Defaults show 50%, 40%, 20%, 17%; Fade increases rightward and exact default retention remains 0.998."
  artifacts:
    - Shared/SimulationState.swift
    - Shared/Renderer.swift
    - Shared/Shaders.metal
    - FluidDynamicsMetalOSX/RenderViewController.swift
    - test_phase05_metal.swift
  key_links:
    - "The Mac slider action uses a narrow Renderer mutation API, which clamps through state.tuning; draw(in:) samples that state for free per-frame uniforms and for every contact batch."
    - "StaticData and BufferData have matching offsets for the new aligned tuning vector; advect reads retention for velocity and density and vorticityConfinement reads swirl."
    - "The same mapping lives in SimulationState.swift, which both apps and the production state tests compile directly."
---

<objective>
As a Mac user, I can open the existing fluid HUD and adjust four labeled sliders while watching the simulation respond immediately, without disturbing its original behavior at the defaults.

Purpose: Deliver the first production end-to-end tuning tracer, from native Mac controls through bounded shared state to real Metal simulation passes, with behavioral tests.
Output: Shared mapping and renderer/shader wiring, a working Mac Tuning disclosure, and focused state/GPU/UI checks.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-fluid-tuning/05-CONTEXT.md
@.planning/phases/05-live-fluid-tuning/05-RESEARCH.md
@.planning/phases/05-live-fluid-tuning/05-UI-SPEC.md
@.planning/phases/05-live-fluid-tuning/05-VALIDATION.md
@.planning/phases/05-live-fluid-tuning/05-PATTERNS.md
@.planning/phases/04-discoverable-visualization/04-UI-SPEC.md
@.planning/phases/03-reliable-simulation-state/03-CONTEXT.md
@.planning/phases/01-modern-mac-baseline/01-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run a live Mac Tuning slider through shared state and Metal</name>
  <files>Shared/SimulationState.swift, Shared/Renderer.swift, Shared/Shaders.metal, FluidDynamicsMetalStateTests/SimulationStateTests.swift, FluidDynamicsMetalOSX/RenderViewController.swift</files>
  <read_first>Shared/SimulationState.swift, Shared/Renderer.swift, Shared/Shaders.metal, FluidDynamicsMetalStateTests/SimulationStateTests.swift, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetal.xcodeproj/project.pbxproj, .planning/phases/05-live-fluid-tuning/05-CONTEXT.md, .planning/phases/05-live-fluid-tuning/05-UI-SPEC.md, .planning/phases/05-live-fluid-tuning/05-VALIDATION.md, .planning/phases/05-live-fluid-tuning/05-PATTERNS.md</read_first>
  <action>First add production-state XCTest cases for all four mapping endpoints, interior positions and inverse default positions; run the Mac test command to see the new mapping assertions fail, then implement a pure shared slider mapping in `Shared/SimulationState.swift` (already a source of both apps and the test target). Position `p` is clamped to 0...1, displayed as rounded `100*p%`. Force, Dye and Swirl store `2*p`; their defaults read 50%, 40%, 20%. Fade uses the exact piecewise mapping from `05-UI-SPEC.md`: 0% -> 0.9995, 17% -> 0.998, 100% -> 0.9905 retention, with continuous inverse and no underlying integer-percent quantization; reject non-finite inputs through existing tuning bounds. Provide a narrow `Renderer` setter for these four controls, updating only `state.tuning` on the UI/main thread; no second solver-value cache. In `draw(in:)`, snapshot tuning once per running frame: `writeContacts` scales each contact's vector impulse by force, sets scalar impulse to `(dye, 0)`, and applies those rules again for every batch after the first 10 contacts. Leave `touchRadius` and 40 pressure iterations unchanged; taps have zero vector and still deposit dye. Extend Swift `StaticData` and Metal `BufferData` identically with an aligned `simd_float4`/`float4` carrying retention in `.x` and swirl in `.y`; initialize it in all three free uniform buffers and update only the chosen buffer in `nextBuffer` before command encoding, including when there are no contacts. In `Shaders.metal`, replace `0.998h` in `advect` with half-precision multiplication by this retention for both velocity and density, and `float2(0.4, 0.4)` in `vorticityConfinement` with the uniform swirl. Do not update in-flight GPU buffers when the UI changes tuning or step a paused frame. Add a first usable Mac `Show Tuning` / `Hide Tuning` native button after Pause/Resume within the existing HUD and four continuous labeled `NSSlider`s reading/writing the renderer mapping with integer-percent text (including the Fade helper); starts collapsed, stays expanded until manually closed. Reuse the existing material and actual-bounds input exclusion; keep canvas outside the HUD usable. Follow Task 2 for constrained layout, focused-Space and UI regression coverage; this task must already let a Mac user open a slider and see a live changed fluid response.</action>
  <verify><automated>xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO</automated></verify>
  <acceptance_criteria>
    - New `SimulationStateTests.swift` mapping cases execute in the Mac test run and assert Force/Dye/Swirl default 0.5/0.4/0.2, Fade default 0.17, Fade retention 0.9995/0.998/0.9905 at 0/0.17/1, and round-trip values within Float tolerance.
    - `Renderer.draw(in:)` uses per-running-frame `state.tuning` for vector/scalar input independently in `writeContacts` (including extra contact batches), and the paused branch still renders without solver passes or contact application.
    - Swift `StaticData` and Metal `BufferData` retain matching field order and use the new vector in both advection and vorticity confinement; no shader entry point, radius or pressure-iteration count changes.
    - The Mac HUD has `Show Tuning`, `Hide Tuning`, `Force`, `Dye`, `Swirl`, `Fade` and `Higher Fade = faster fade`, with four live percentage readouts; expanded state survives canvas strokes.
  </acceptance_criteria>
  <done>A Mac slider can change the real running simulation; original bounded defaults and all four user-facing mappings are executable and shared with iOS.</done>
</task>

<task type="auto">
  <name>Task 2: Prove GPU behavior and finish compact Mac HUD interaction</name>
  <files>FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetalOSXUITests/HUDUITests.swift, test_phase05_metal.py, test_phase05_metal.swift</files>
  <read_first>FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetalOSXUITests/HUDUITests.swift, Shared/SimulationState.swift, Shared/Renderer.swift, Shared/Shaders.metal, test_phase03_metal.py, test_phase03_metal.swift, .planning/phases/05-live-fluid-tuning/05-UI-SPEC.md, .planning/phases/05-live-fluid-tuning/05-VALIDATION.md, .planning/phases/04-discoverable-visualization/04-UI-SPEC.md</read_first>
  <action>Build the Mac Debug app and add `test_phase05_metal.py`/`.swift` following the existing Phase 3 offscreen harness: locate the freshly built Mac `default.metallib`, encode `applyForceVector`, `applyForceScalar`, `advect` and `vorticityConfinement` into RG16F textures with a buffer matching `StaticData`/`BufferData`, and assert readback within Float16 tolerance. Use a nonzero contact and compare default (force 1, dye 0.8) against force 0/dye above 0 and dye 0/force above 0; use nonempty velocity and density with no contacts to check retention changes *both* fields and a nonuniform nonzero vorticity field to check swirl 0 versus 0.4 without new input. Include an explicit default-versus-original-constants check, not just source-string checks. Finish Mac layout inside the same HUD: a bounded max-320-pt width and available content height minus 32 pt, retained upper-trailing 16 pt anchor and 320x240 window minimum, full labels, one internal scroll area when the entire control stack overflows, otherwise scroll only the tuning rows; no nested competing scrollers in compact mode. Keep `View: {field}` visible if field choices scroll away, preserve native material and Reduce Transparency fallback, and preserve focus order Field -> Pause -> disclosure -> four sliders. Include the disclosure button in focused-Space routing; let native arrows adjust a focused slider without Space/S affecting tuning or pausing unexpectedly. Extend Mac `HUDUITests.swift` with a fresh-launch closed disclosure assertion, all four default slider accessibility names/percentage values when opened, one slider drag to a different percentage, Hide/Reopen retains it, paused field selection and Space on focused disclosure do not double-toggle; retain existing HUD regression cases and an off-HUD canvas shortcut check.</action>
  <verify><automated>python3 -m unittest test_phase05_metal.py</automated></verify>
  <acceptance_criteria>
    - The Mac Debug build followed by `python3 -m unittest test_phase05_metal.py` exits 0 and executes GPU assertions for both independent input zeros, default impulses, changed retention on nonempty velocity/density and changed swirl with no fresh contacts.
    - `xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO` executes new and existing HUD UI tests and state tests successfully.
    - The Mac Tuning disclosure starts closed and keeps its local expanded state across field choice, pause, resize and canvas interaction; only the actual bounded HUD intercepts pointer input and every control is scroll-reachable at 320x240.
    - Default accessibility values read Force `50%`, Dye `40%`, Swirl `20%`, Fade `17%`; focused Space on a disclosure acts once, and arrows adjust focused native sliders.
  </acceptance_criteria>
  <done>The Mac end-to-end tracer is covered by real shader readback and native HUD tests, including tight window layout and existing shortcuts.</done>
</task>

</tasks>

<threat_model>
Local UI controls only: no network, files or persistence. Input-integrity risk is a slider click creating a fluid contact, while stale or in-flight uniforms risk changing the wrong frame; bounded HUD hit-testing, a single state snapshot per running frame and writes only to a free buffer mitigate both. High-severity external threats: none identified.
</threat_model>

<verification>
- [ ] The Mac test scheme executes the new mapping and UI cases, with existing Phase 4 HUD cases still passing.
- [ ] The Mac Debug app builds before the Phase 5 GPU harness runs against its new `default.metallib`; GPU readback verifies changed values, not only state or source strings.
- [ ] Mac live observation of default appearance, input independence, changing swirl/fade without new contact and a short window is recorded in Plan 03; compilation is not claimed as visual proof.
</verification>

<success_criteria>
- The Mac user has four bounded live native controls inside the existing HUD; both input-specific and running-fluid effects reach real Metal passes with original-looking defaults (CTRL-02, CTRL-03, CTRL-04; D-01–D-10).
</success_criteria>

<output>
Create `.planning/phases/05-live-fluid-tuning/05-01-SUMMARY.md` with Mac test counts, GPU readback evidence and any outstanding manual appearance/compact-layout observations.
</output>
