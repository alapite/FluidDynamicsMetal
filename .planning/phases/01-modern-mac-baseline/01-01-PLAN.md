---
phase: 01-modern-mac-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FluidDynamicsMetal.xcodeproj/project.pbxproj
  - build-macos.sh
  - .gitignore
  - README.md
  - .planning/phases/01-modern-mac-baseline/01-BASELINE.md
autonomous: false
requirements:
  - PLAT-01
user_setup:
  - Xcode Metal Toolchain (install with `xcodebuild -downloadComponent MetalToolchain` if absent)
  - Metal-capable Apple Silicon Mac with desktop session; macOS 26 host for actual 26 runtime sign-off
must_haves:
  truths:
    - A Mac app targeting macOS 26 builds as arm64 with Xcode without a deployment-target override (PLAT-01).
    - Dragging the mouse on the launched Metal canvas deposits visible density and imparts motion (PLAT-01).
    - The default look and interactions have a reproducible reference for later parity checks (PLAT-01).
  artifacts:
    - FluidDynamicsMetal.xcodeproj/project.pbxproj
    - build-macos.sh
    - FluidDynamicsMetalOSX.app
    - .planning/phases/01-modern-mac-baseline/01-BASELINE.md
    - FluidDynamicsMetal.gif
  key_links:
    - FluidDynamicsMetalOSX/RenderViewController.swift mouseDragged(with:) passes positions to Shared/Renderer.swift updateInteraction(points:in:).
    - Shared/Renderer.swift draw(in:) uses Shared/Shaders.metal applyForceVector, applyForceScalar and visualizeScalar.
    - FluidDynamicsMetal.xcodeproj/project.pbxproj Mac Debug and Release deployment targets both equal 26.0.
---

<objective>
As an Apple Silicon Mac user, I want to launch and stir the original fluid on macOS 26 so I can use and recognize the simulation.

Purpose: Establish a real Mac user-input-to-GPU walking skeleton and a recorded visual baseline for subsequent phases.
Output: A Mac scheme that builds without local deployment overrides and a checked, reproducible `01-BASELINE.md`.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
@/Users/abiola/.config/opencode/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-modern-mac-baseline/01-RESEARCH.md
@.planning/phases/01-modern-mac-baseline/01-PATTERNS.md
@.planning/phases/01-modern-mac-baseline/01-VALIDATION.md
@.planning/phases/01-modern-mac-baseline/01-SKELETON.md
@FluidDynamicsMetal.xcodeproj/project.pbxproj
@FluidDynamicsMetalOSX/RenderViewController.swift
@Shared/Renderer.swift
@Shared/Shaders.metal
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deliver a runnable Apple Silicon Mac fluid slice</name>
  <files>FluidDynamicsMetal.xcodeproj/project.pbxproj, build-macos.sh, .gitignore, README.md; Mac/Shared Swift or Metal files ONLY if the build identifies a specific blocking diagnostic</files>
  <read_first>FluidDynamicsMetal.xcodeproj/project.pbxproj, build-macos.sh (if present), .gitignore (if present), README.md, FluidDynamicsMetalOSX/RenderViewController.swift, Shared/Renderer.swift, Shared/Shaders.metal, Shared/MetalDevice.swift, .planning/phases/01-modern-mac-baseline/01-RESEARCH.md</read_first>
  <action>Check for the Xcode Metal Toolchain: if `metal` cannot execute, run `xcodebuild -downloadComponent MetalToolchain` and retry. In the Mac target Debug and Release build settings of `FluidDynamicsMetal.xcodeproj/project.pbxproj`, set `MACOSX_DEPLOYMENT_TARGET = 26.0` and `SWIFT_VERSION = 5.0`; leave the iOS target's settings for Phase 2. Build `FluidDynamicsMetalOSX` for `platform=macOS,arch=arm64` in Debug with `CODE_SIGNING_ALLOWED=NO` and NO deployment-target override. If a compiler error appears, fix the exact diagnostic in the minimal Mac or Shared file, retaining the `RenderViewController` mouseDown/mouseDragged/mouseUp → `Renderer.updateInteraction` path, `Renderer.draw(in:)` 40 pressure iterations, shader entry-point names, `StaticData`/`BufferData` layout and existing numerical constants. Rebuild after each fix. Add root-level `build-macos.sh` that runs the no-override arm64 Debug build using ignored `.build/` as DerivedData and places the resulting `FluidDynamicsMetalOSX.app` in the project root using `ditto`; ignore both output paths in `.gitignore` and document the command in `README.md`. Verify the root bundle contains the default Metal library, its executable includes arm64, and its minimum macOS is 26.0.</action>
  <verify><automated>./build-macos.sh</automated></verify>
  <acceptance_criteria>
    - Mac target Debug and Release in `project.pbxproj` specify `MACOSX_DEPLOYMENT_TARGET = 26.0` and `SWIFT_VERSION = 5.0`; iOS target settings remain outside this task.
    - The stated arm64 Debug build exits 0 without `MACOSX_DEPLOYMENT_TARGET` on the command line.
    - `./build-macos.sh` exits 0 and places `FluidDynamicsMetalOSX.app` in the root; its `Contents/Resources/default.metallib` exists, `lipo -archs` lists `arm64`, and its bundle minimum OS is 26.0.
  </acceptance_criteria>
  <done>The Mac fluid app builds for arm64 without overrides and has its Metal shader library available at launch.</done>
</task>

<task type="auto">
  <name>Task 2: Publish a reproducible default-look reference</name>
  <files>.planning/phases/01-modern-mac-baseline/01-BASELINE.md</files>
  <read_first>.planning/phases/01-modern-mac-baseline/01-RESEARCH.md, .planning/phases/01-modern-mac-baseline/01-VALIDATION.md, .planning/phases/01-modern-mac-baseline/01-SKELETON.md, README.md, FluidDynamicsMetal.gif, Shared/Renderer.swift, Shared/Shaders.metal, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetal.xcodeproj/project.pbxproj</read_first>
  <action>Create `01-BASELINE.md` with Xcode/SDK and test host OS/architecture, the exact no-override arm64 build command and local `.app` launch instructions. Link the tracked `FluidDynamicsMetal.gif` as the original visual reference. Record current code-backed defaults: density is initial displayed slab, dark-blue scalar visualization RGB `(0.0, 0.06, 0.19)`, `ScreenScaleAdjustment = 1.0`, `inkRadius = 150`, scalar impulse `(0.8, 0.0)`, advection `0.998h`, curl `(0.4, 0.4)`, and 40 pressure iterations. Document drag-to-add-density/force, Space pause, S density → pressure → velocity → vorticity. Add a table with separate `build`, `launch`, `drag`, `Space`, `S`, `macOS 26 runtime`, and `screenshot` rows showing observed PASS/FAIL/NOT TESTED and evidence; never report unperformed steps as passed. A screenshot is optional: capture and link one only if desktop access and recording permission are already available; do not request screen/audio recording permissions. Keep the original GIF link when no screenshot exists.</action>
  <verify><automated>rg -q 'macOS 26 runtime|NOT TESTED' .planning/phases/01-modern-mac-baseline/01-BASELINE.md</automated></verify>
  <acceptance_criteria>
    - `01-BASELINE.md` links `FluidDynamicsMetal.gif` and contains exact build/run steps, default source values, and the field order density → pressure → velocity → vorticity.
    - `01-BASELINE.md` has explicit observed result/evidence rows for build, launch, drag, Space, S, macOS 26 runtime, and screenshot; unknown outcomes say `NOT TESTED`.
    - A screenshot link is present only if the screenshot file exists; any claimed macOS 26 success identifies the actual macOS 26 host.
  </acceptance_criteria>
  <done>Later phases can reconstruct the original look, user input and verification status from the recorded baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm live fluid at phase closeout</name>
  <files>.planning/phases/01-modern-mac-baseline/01-BASELINE.md</files>
  <read_first>.planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/01-modern-mac-baseline/01-VALIDATION.md, FluidDynamicsMetal.gif</read_first>
  <action>At phase closeout, when the user is ready for manual verification, collect observed results for launch, drag, Space and S on Apple Silicon; record OS version and actual macOS 26 launch/drag evidence separately in `01-BASELINE.md`. Mark any step that was not run `NOT TESTED` and do not approve phase runtime compatibility solely from a macOS 27 host. No screen or audio recording permission is needed for the visual check.</action>
  <what-built>The arm64 macOS 26-targeting Metal app and a default-look reference document.</what-built>
  <how-to-verify>When ready at phase closeout, open the built `.app` from the full DerivedData path in `01-BASELINE.md`. Drag across the canvas and release: visible blue density should appear, move and fade. Press Space twice: animation stops then resumes. Press S four times: pressure, velocity, vorticity and density appear in that order. Compare with `FluidDynamicsMetal.gif` and check `01-BASELINE.md` accurately records outcomes. For macOS 26 runtime sign-off, repeat launch/drag on an actual macOS 26 host and record that host's OS version. Report any failed step rather than approving an untested result; no screenshot is required.</how-to-verify>
  <verify>Review observed-result/evidence rows in `01-BASELINE.md` and confirm that each `PASS` reflects an actually performed check on the recorded OS.</verify>
  <acceptance_criteria>
    - `01-BASELINE.md` records an observed result for launch and drag on an Apple Silicon Mac.
    - Any `macOS 26 runtime` PASS identifies a macOS 26 host; otherwise that result remains `NOT TESTED`.
  </acceptance_criteria>
  <done>The observer approves the recorded behavior or reports a specific failure for correction.</done>
  <resume-signal>Type "approved" after these observations and updating `01-BASELINE.md`, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
No new network, identity, storage or untrusted-data boundary is introduced. The existing local mouse-to-GPU path remains the only input path; retain the current `Renderer` and shader uniform layout while adjusting build settings. High-severity threats: none identified for this local build-compatibility slice.
</threat_model>

<verification>
- [ ] Arm64 Debug build of `FluidDynamicsMetalOSX` succeeds with `CODE_SIGNING_ALLOWED=NO` and without deployment-target override.
- [ ] Built app contains a Metal library, arm64 executable, and macOS 26 minimum; a real macOS 26 host is used before claiming macOS 26 runtime success.
- [ ] Manual drag, Space and S observations are recorded accurately; reference links to `FluidDynamicsMetal.gif` and any actual screenshot.
</verification>

<success_criteria>
- A user can launch the macOS 26-targeting Mac app and stir visible familiar density on an Apple Silicon Mac.
- The Mac build requires no project-local deployment override and its baseline is available for future parity checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-modern-mac-baseline/01-01-SUMMARY.md` with build and manual verification evidence.
</output>
