---
phase: 02-modern-ios-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FluidDynamicsMetal.xcodeproj/project.pbxproj
  - FluidDynamicsMetaliOS/Info.plist
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
  - SIM-01
must_haves:
  truths:
    - The iOS 26 app launches on an iPhone 26 simulator and a user can drag to create density on its full Metal canvas (PLAT-02, SIM-01).
    - Both schemes build in Debug with Xcode 27 without a Swift 4 mode or command-line deployment-target override (PLAT-03).
    - The Mac retains its dark-blue density default and original mouse/Space/S input; the iOS scheme retains its existing double-tap recognizers (D-04, D-06, D-08, D-10).
  artifacts:
    - FluidDynamicsMetal.xcodeproj/project.pbxproj
    - FluidDynamicsMetaliOS/Info.plist
    - FluidDynamicsMetaliOS.app
  key_links:
    - FluidDynamicsMetaliOS/Base.lproj/Main.storyboard root MTKView installs the shared Renderer in FluidDynamicsMetaliOS/RenderViewController.swift.
    - Shared/Renderer.swift draw(in:) applies input forces then visualizes density with Shared/Shaders.metal visualizeScalar.
    - Mac and iOS schemes each compile Shared/Renderer.swift and Shared/Shaders.metal from the same source files.
---

<objective>
As an iPhone user, I want to launch and drag through the familiar blue fluid on iOS 26 so that the existing Mac experience is available on my phone.

Purpose: Deliver the smallest runnable iOS 26 input → shared simulation → drawable tracer, plus the required both-scheme build gate before improving multi-touch.
Output: Modern iOS Xcode settings, installable simulator bundle, first iPhone launch/drag and regression-free Mac build.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-modern-ios-parity/02-CONTEXT.md
@.planning/phases/02-modern-ios-parity/02-RESEARCH.md
@.planning/phases/02-modern-ios-parity/02-VALIDATION.md
@.planning/phases/02-modern-ios-parity/02-PATTERNS.md
@.planning/phases/01-modern-mac-baseline/01-BASELINE.md
@FluidDynamicsMetal.xcodeproj/project.pbxproj
@FluidDynamicsMetaliOS/Info.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch an iOS 26 Metal fluid tracer with current Swift</name>
  <files>FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetaliOS/Info.plist</files>
  <read_first>FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetaliOS/Info.plist, FluidDynamicsMetaliOS/Base.lproj/Main.storyboard, FluidDynamicsMetaliOS/RenderViewController.swift, Shared/Renderer.swift, Shared/Shaders.metal, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/02-modern-ios-parity/02-CONTEXT.md</read_first>
  <action>In `project.pbxproj` set `IPHONEOS_DEPLOYMENT_TARGET = 26.0` and `SWIFT_VERSION = 5.0` for the project Debug/Release and the `FluidDynamicsMetaliOS` target Debug/Release; remove `SWIFT_SWIFT3_OBJC_INFERENCE = Default` from iOS configs. Keep `FluidDynamicsMetalOSX` target Debug/Release at `MACOSX_DEPLOYMENT_TARGET = 26.0`, `SWIFT_VERSION = 5.0`. In `FluidDynamicsMetaliOS/Info.plist`, replace the obsolete `armv7`-only `UIRequiredDeviceCapabilities` array with the `metal` capability supported by this Metal-only app; retain iPhone/iPad families (`TARGETED_DEVICE_FAMILY = "1,2"`) and the root full-canvas MTKView. Build `FluidDynamicsMetaliOS` for `platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4` in Debug with `CODE_SIGNING_ALLOWED=NO`, without `IPHONEOS_DEPLOYMENT_TARGET` or `SWIFT_VERSION` on the command line. If this Xcode emits a Swift/Metal compile diagnostic, repair only the specific issue while keeping the Phase 1 constants, Swift `StaticData` / Metal `BufferData` alignment and shader entry-point names; rerun the build. Boot the named simulator, install the *freshly built* `FluidDynamicsMetaliOS.app` using its `TARGET_BUILD_DIR` and `WRAPPER_NAME` from `xcodebuild -showBuildSettings` (or a fixed `-derivedDataPath`), and launch `ro.andreisergiupitis.FluidDynamicsMetaliOS` via `xcrun simctl`. Do not treat a successful compile as a visual drag observation; record that for Plan 03.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - Project and iOS target Debug/Release configurations each specify `IPHONEOS_DEPLOYMENT_TARGET = 26.0` and `SWIFT_VERSION = 5.0`; no effective iOS configuration uses Swift 4 or `SWIFT_SWIFT3_OBJC_INFERENCE`.
    - `FluidDynamicsMetaliOS/Info.plist` has `metal` rather than `armv7` in `UIRequiredDeviceCapabilities`; the simulator `.app` has `MinimumOSVersion = 26.0` and a `default.metallib`.
    - The no-override iOS Debug build exits 0, and `xcrun simctl launch` of its freshly installed bundle exits 0 on an iOS 26 iPhone simulator; visual drag remains subject to observation.
  </acceptance_criteria>
  <done>A current-Swift iOS fluid app builds, installs and launches on the required simulator.</done>
</task>

<task type="auto">
  <name>Task 2: Establish both-scheme build parity before input changes</name>
  <files>FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetaliOS/Info.plist</files>
  <read_first>FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetaliOS/Info.plist, build-macos.sh, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetaliOS/RenderViewController.swift, Shared/Renderer.swift, .planning/phases/02-modern-ios-parity/02-VALIDATION.md</read_first>
  <action>Run the Mac Debug build for `platform=macOS,arch=arm64` with `CODE_SIGNING_ALLOWED=NO`, no deployment-target override, after the iOS build; inspect effective settings of each target and the iOS bundle's version and shader library. If the iOS-target change regresses the Mac target, correct the shared *project configuration* without modifying Mac input or simulation defaults. Capture the two exact commands and results in this plan's SUMMARY.md; the Mac runtime interaction reference is `01-BASELINE.md` on macOS 27, not a new macOS 26 runtime claim.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - Both specified Debug builds exit 0 without local `IPHONEOS_DEPLOYMENT_TARGET` or `MACOSX_DEPLOYMENT_TARGET` overrides.
    - Effective Mac target build settings still use `MACOSX_DEPLOYMENT_TARGET = 26.0` and `SWIFT_VERSION = 5.0`; the Mac controller still handles mouseDown/mouseDragged/mouseUp, Space and S.
    - SUMMARY.md records actual build results; it does not claim unobserved simulator drag or macOS 26 runtime behavior.
  </acceptance_criteria>
  <done>The new iOS tracer builds alongside the unchanged working Mac target.</done>
</task>

</tasks>

<threat_model>
Local iOS touch is the only new input source; no network, data storage or credentials. The build and launch slice introduces no external trust boundary. Bounds/slot safety of multi-touch is addressed by Plan 02, which must avoid copying unbounded positions into the five-element GPU struct. High-severity threats: none identified for this local app build slice.
</threat_model>

<verification>
- [ ] iOS no-override Debug build, iOS 26 simulator install/launch and iOS bundle minimum/Metal library checks succeed.
- [ ] Mac no-override arm64 Debug build succeeds with existing Space/S/mouse path retained.
- [ ] Phase 1 density shader constants and the full-screen iOS Metal view remain unchanged.
</verification>

<success_criteria>
- A current-Xcode, iOS 26-targeting phone app runs the original touch-to-Metal tracer alongside a buildable Mac app; visual behavior is checked during Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modern-ios-parity/02-01-SUMMARY.md` with the commands, bundle evidence and actual simulator launch result.
</output>
