---
phase: 02-modern-ios-parity
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - .planning/phases/02-modern-ios-parity/02-PARITY.md
autonomous: false
requirements:
  - PLAT-02
  - PLAT-03
  - SIM-01
must_haves:
  truths:
    - The current iOS build is launched and visually assessed on both iOS 26 iPhone and iPad simulators, including drag and existing gestures (D-03, D-04, D-05, D-06, D-08, D-09, PLAT-02, SIM-01).
    - Density looks recognizably dark-blue on black, swirls and fades, and the stroke covers a comparable fraction of both canvases (D-06, D-07).
    - The Mac build still succeeds, the Phase 1 Mac reference is used for parity, and macOS 26 runtime is not misreported as verified (D-10, PLAT-03).
    - Multi-touch observations are recorded as simulator-observed or explicitly unverified/device-only, never claimed as physical-device coverage (D-01, D-02, D-11).
  artifacts:
    - .planning/phases/02-modern-ios-parity/02-PARITY.md
  key_links:
    - The iPhone and iPad installed bundles are produced by FluidDynamicsMetaliOS no-override Debug builds, not stale DerivedData copies.
    - Recorded visual results are compared with .planning/phases/01-modern-mac-baseline/01-BASELINE.md and FluidDynamicsMetal.gif.
---

<objective>
As an iPhone or iPad user, I want the newly modernized fluid to launch and respond on both layouts so that the app feels like the familiar Mac experience.

Purpose: Validate the vertical slice in actual iOS 26 simulators, distinguish observed outcomes from unsupported multi-touch assumptions, and record reproducible platform parity evidence.
Output: `02-PARITY.md` with build, bundle, simulator and Mac-reference results plus a human interaction checkpoint.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
@/Users/abiola/.config/opencode/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-modern-ios-parity/02-CONTEXT.md
@.planning/phases/02-modern-ios-parity/02-RESEARCH.md
@.planning/phases/02-modern-ios-parity/02-VALIDATION.md
@.planning/phases/01-modern-mac-baseline/01-BASELINE.md
@FluidDynamicsMetal.gif
@FluidDynamicsMetal.xcodeproj/project.pbxproj
@FluidDynamicsMetaliOS/RenderViewController.swift
@FluidDynamicsMetalOSX/RenderViewController.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run both builds and launch fresh iPhone/iPad simulator bundles</name>
  <files>.planning/phases/02-modern-ios-parity/02-PARITY.md</files>
  <read_first>.planning/phases/02-modern-ios-parity/02-VALIDATION.md, .planning/phases/02-modern-ios-parity/02-RESEARCH.md, .planning/phases/02-modern-ios-parity/02-CONTEXT.md, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetaliOS/Info.plist, FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetalOSX/RenderViewController.swift</read_first>
  <action>Build the iOS Debug scheme for `platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4` and the Mac Debug scheme for `platform=macOS,arch=arm64`, both with `CODE_SIGNING_ALLOWED=NO` and no local deployment-target or Swift-version overrides. Identify the fresh iOS `.app` through `xcodebuild -showBuildSettings` (`TARGET_BUILD_DIR` + `WRAPPER_NAME`) or use a dedicated `-derivedDataPath`. Verify bundle `MinimumOSVersion=26.0` via `/usr/libexec/PlistBuddy` and `default.metallib` exists. Boot an available iOS 26.4 iPhone 17 Pro and iPad Pro 11-inch (M5) with `xcrun simctl boot`/`bootstatus`; install and launch `ro.andreisergiupitis.FluidDynamicsMetaliOS` on *both* using `xcrun simctl install`/`launch`. Create `02-PARITY.md` with Xcode, macOS host, simulator OS/device IDs, exact build/install/launch commands, bundle evidence and a separate PASS/FAIL/NOT TESTED row for iPhone launch, iPad launch, iPhone drag, iPad drag, blue-on-black default, swirl/fade, tap dye, one-finger double tap, two-finger double tap, clean shortcut density, concurrent touches, one-touch lift/cancel, proportional stroke, Mac rebuild, Mac mouse/Space/S reference and actual macOS 26 runtime. Record only results already performed and leave visual/manual rows `NOT TESTED` until the next checkpoint. Cite `01-BASELINE.md` and the original `FluidDynamicsMetal.gif` as comparison references.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - Both no-override Debug builds exit 0; the iOS app reports `MinimumOSVersion = 26.0` and includes `default.metallib`.
    - iPhone and iPad iOS 26 simulators each report a successful launch of the freshly installed `ro.andreisergiupitis.FluidDynamicsMetaliOS` app, or a specific launch failure is recorded instead of PASS.
    - `02-PARITY.md` includes host, simulator IDs, commands, separate observed-result/evidence rows and explicit `NOT TESTED` for visual/touch and macOS 26 checks that have not occurred.
  </acceptance_criteria>
  <done>Platform build/install/launch evidence is repeatable, and the remaining visual behaviors are ready for honest sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Observe fluid and existing inputs on iPhone and iPad</name>
  <files>.planning/phases/02-modern-ios-parity/02-PARITY.md</files>
  <read_first>.planning/phases/02-modern-ios-parity/02-PARITY.md, .planning/phases/02-modern-ios-parity/02-CONTEXT.md, .planning/phases/02-modern-ios-parity/02-VALIDATION.md, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, FluidDynamicsMetal.gif, README.md</read_first>
  <action>On the running iOS 26 iPhone and iPad simulators, visually verify the initial density view is unobstructed blue on black; drag and release to see stirring, dye, swirl and fade; compare stroke width as a *fraction* of the short canvas side. Check a brief stationary single tap adds density and a prompt drag stirs, then one-finger double tap pauses/resumes without a dye blot and two-finger double tap cycles density→pressure→velocity→vorticity→density without a dye blot. Where simulator multi-touch is convincing, test two independent simultaneous drags, lifting one, then cancellation; otherwise record these as `NOT TESTED — device-only` (D-11) rather than blocking simulator sign-off or claiming a device pass. Confirm the Mac build and `01-BASELINE.md`'s recorded macOS 27 mouse/Space/S check as the reference; mark a *new* Mac runtime check PASS only if it is actually re-observed. Leave actual macOS 26 runtime `NOT TESTED` until milestone closeout. Update each `02-PARITY.md` row with observed PASS/FAIL/NOT TESTED, OS and device evidence and failed-step details; do not convert unsupported observations to PASS.</action>
  <what-built>A two-platform fluid app with a runnable iOS 26 simulator build, stable multi-touch path, existing gestures and recorded build/launch evidence.</what-built>
  <how-to-verify>With each simulator on screen, drag once, stop, briefly tap, then exercise one-finger double tap and (where feasible) two-finger double tap. Look for a blue dye trail which keeps moving and fading against black; compare the iPad trail's fractional width with iPhone. Attempt simultaneous finger input only if simulator touch tools can reproduce it convincingly; otherwise say unverified/device-only. Use the result table in `02-PARITY.md` to distinguish direct observation from a build-only or prior-Mac baseline result. Type "approved" after recording the observed results, or describe any failed interaction for correction.</how-to-verify>
  <verify>Review `02-PARITY.md` for actual iPhone and iPad launch, drag and default-look observations; do not approve any claimed physical-device or macOS 26 runtime result without those hosts.</verify>
  <acceptance_criteria>
    - Each iOS 26 simulator layout has a recorded observed launch and drag/default-look result with device name, OS and evidence.
    - iOS single/double tap and clean-shortcut results distinguish observed behavior from simulator limitations; multi-finger claims are `NOT TESTED — device-only` if not reproducible in simulator.
    - A Mac interaction PASS cites a newly observed Mac host or the pre-existing Phase 1 macOS 27 reference; actual macOS 26 runtime is `NOT TESTED` pending milestone closeout.
  </acceptance_criteria>
  <done>The observer confirms genuine simulator parity or identifies the exact interaction needing correction, with unverified hardware behavior labelled accurately.</done>
  <resume-signal>Type "approved" after observing and recording results in `02-PARITY.md`, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
No new network or persisted data. The main integrity risk is reporting an unobserved visual or multi-touch result as verified. Distinguish simulator checks, physical device checks, and the deferred macOS 26 runtime in the evidence table. High-severity external threats: none.
</threat_model>

<verification>
- [ ] Both no-override scheme builds pass; iOS bundle has minimum 26.0 and compiled Metal library.
- [ ] Fresh iOS 26 iPhone and iPad simulator bundles install/launch; observed manual behavior is entered in the parity table.
- [ ] Physical multi-touch and macOS 26 runtime checks are not inferred from simulator or macOS 27 evidence.
</verification>

<success_criteria>
- The iOS 26 app visibly stirs familiar blue density on iPhone and iPad simulators while both Xcode schemes build and existing input shortcuts have recorded evidence; unsupported hardware claims remain unverified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modern-ios-parity/02-03-SUMMARY.md` with links to `02-PARITY.md` and exact verification results.
</output>
