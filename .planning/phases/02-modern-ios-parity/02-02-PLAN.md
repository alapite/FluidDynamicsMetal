---
phase: 02-modern-ios-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - FluidDynamicsMetaliOS/RenderViewController.swift
  - Shared/Renderer.swift
  - Shared/Shaders.metal
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
  - SIM-01
must_haves:
  truths:
    - Every simultaneous iPhone/iPad touch that the platform delivers has an independent dye/force position; lifting/cancelling one preserves the others (D-01, D-02, SIM-01).
    - A stationary single tap deposits dye after shortcut ambiguity resolves; moving a finger begins fluid motion promptly, without a double-tap dye mark (D-03, D-05).
    - One-finger double tap pauses/resumes and two-finger double tap cycles density, pressure, velocity and vorticity; Mac mouse/Space/S remain usable (D-04).
    - Initial density is dark-blue on black and the iPad stroke has a comparable canvas-relative width to the iPhone stroke (D-06, D-07, D-08).
  artifacts:
    - FluidDynamicsMetaliOS/RenderViewController.swift
    - Shared/Renderer.swift
    - Shared/Shaders.metal
  key_links:
    - UIKit touch identity and gesture recognition determine the snapshot sent to Renderer, not the changed-touches callback set.
    - Renderer writes a complete fresh StaticData snapshot for each frame; Shaders.metal BufferData has identical contact capacity and following-field offsets.
    - Mac uses the same Renderer and shaders, so both target builds guard shared GPU changes.
---

<objective>
As an iPhone or iPad user, I want to stir with all active fingers and use the existing shortcuts without accidental dye so I can interact naturally with the same fluid as on Mac.

Purpose: Expand the runnable iOS tracer into stable multi-contact input with gesture-safe tap dye and canvas-relative touch footprint, preserving the shared solver and Mac baseline.
Output: Safe iOS touch identity tracking, matching Swift/Metal uniforms, unambiguous tap/drag behavior and both-target builds.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modern-ios-parity/02-CONTEXT.md
@.planning/phases/02-modern-ios-parity/02-RESEARCH.md
@.planning/phases/02-modern-ios-parity/02-VALIDATION.md
@.planning/phases/02-modern-ios-parity/02-PATTERNS.md
@.planning/phases/01-modern-mac-baseline/01-BASELINE.md
@FluidDynamicsMetaliOS/RenderViewController.swift
@FluidDynamicsMetalOSX/RenderViewController.swift
@Shared/Renderer.swift
@Shared/Shaders.metal
</context>

<tasks>

<task type="auto">
  <name>Task 1: Support concurrent, independent touch-to-fluid input</name>
  <files>FluidDynamicsMetaliOS/RenderViewController.swift, Shared/Renderer.swift, Shared/Shaders.metal</files>
  <read_first>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOS/Base.lproj/Main.storyboard, FluidDynamicsMetalOSX/RenderViewController.swift, Shared/Renderer.swift, Shared/Shaders.metal, .planning/phases/02-modern-ios-parity/02-RESEARCH.md, .planning/phases/02-modern-ios-parity/02-CONTEXT.md</read_first>
  <action>Replace `malloc`/`memset`/`memcpy` forwarding in iOS `touchesBegan`/`touchesMoved` with a controller-owned stable identity→slot mapping for every live `UITouch` and position/previous-position sampled using `touch.location(in: metalView)`; deliver a *full* active snapshot after every callback. On `touchesEnded`/`touchesCancelled`, remove only the affected identities and resubmit survivors; cancel any pending delayed tap for ended/cancelled touches as appropriate. Do not reorder survivors or give a newly started finger a recycled finger's prior coordinate: initial velocity for a newly added touch is zero. Update `Shared/Renderer.swift` with a touch-specific interaction entrypoint accepting independent positions/deltas while preserving the existing Mac `updateInteraction(points:in:)` entrypoint. Expand the fixed contact uniform capacity from five to ten by changing Swift `StaticData.positions`/`.impulses` and Metal `BufferData.positions`/`.impulses` plus both `applyForceVector` and `applyForceScalar` loop bounds together; account for matching strides and offsets of `impulseScalar`, `offsets`, `screenSize`, and `inkRadius`. Never copy more entries than allocated; if hardware supplies more than ten simultaneously, handle overflow via bounded extra splat passes or an equivalent all-contacts strategy rather than dropping contacts. Each `nextBuffer` call must write current positions, impulses and scalar impulse *including zeros on nil/empty input* so released/cancelled touches cannot keep injecting force. Preserve 40 pressure iterations, shader function names, blue visualization, fade and curl constants. After each shared-code change, build both schemes using the Plan 01 no-override commands.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - Controller tracks touch identity and passes the complete active touch snapshot; `touchesEnded` and `touchesCancelled` do not forward a global nil while other contacts remain.
    - No `malloc`/`memcpy`/`memset` of touch positions remains; iOS runtime input does not write past allocated uniform slots even with >5 contacts.
    - Swift `StaticData` and Metal `BufferData` both have matching ten-position/ten-impulse layout and shaders visit each active slot; empty input writes zeros for positions, impulses and scalar impulse.
    - The iOS no-override build and Mac arm64 no-override build both exit 0; the Mac controller's mouse/Space/S entrypoints still call Renderer.
  </acceptance_criteria>
  <done>Concurrent fingers are represented independently in the shared GPU input path without stale-force or memory-overflow hazards.</done>
</task>

<task type="auto">
  <name>Task 2: Make tap shortcuts dye-free and scale iPad strokes</name>
  <files>FluidDynamicsMetaliOS/RenderViewController.swift, Shared/Renderer.swift</files>
  <read_first>FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetaliOS/Base.lproj/Main.storyboard, Shared/Renderer.swift, Shared/Shaders.metal, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/02-modern-ios-parity/02-CONTEXT.md, .planning/phases/02-modern-ios-parity/02-VALIDATION.md</read_first>
  <action>Keep the one-finger double-tap `doubleTap()` pause and two-finger double-tap `changeSource()` actions; arbitrate their tap stream before injecting stationary dye. Register a one-finger single-tap recognizer whose successful action deposits a one-shot stationary splat only after *both* existing double-tap recognizers fail (`require(toFail:)` relationships); gate touch-down candidate dye until this resolution. For a held stationary finger, inject density after the tap ambiguity window via a cancellable delayed action or a short stationary-hold recognizer, cancelling it on shortcut recognition, end/cancel or conversion to dragging. Forward moving touches immediately after crossing a small movement threshold rather than waiting for double-tap failure, while maintaining the stable active-slot snapshot and never injecting the two down/up splats of a recognized double tap. Ensure the one-finger and two-finger double-tap recognizers do not both fire for one two-finger shortcut. For iOS only, set uniform `inkRadius` from `150 * pow(min(metalView.bounds.width, metalView.bounds.height) / 375, 2)` (minimum positive bound) at the point where iOS interaction uses it; retain the Mac's `150 / Renderer.ScreenScaleAdjustment` default and the shader's Gaussian formula. Keep the MTKView full-screen with no overlay and initial density `currentIndex=0`. Build both targets after adjusting shared Renderer code.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - A stationary quick single tap reaches the dye path only after the one- and two-finger double-tap recognizers fail; recognized shortcuts never intentionally forward their touch-down as dye.
    - A moved finger reaches Renderer without waiting for the double-tap recognition timeout; when a finger lifts, other active positions remain forwarded and release clears stale force.
    - One-finger double tap still calls pause exactly once; two-finger double tap calls `nextSlab()` exactly once; no named on-screen controls or overlays are introduced.
    - The iOS Gaussian squared-distance denominator scales with the *square* of the short canvas side relative to 375 points, while the Mac default remains 150; both iOS and Mac no-override Debug builds exit 0.
  </acceptance_criteria>
  <done>Everyday drag and tap input is responsive, shortcuts leave clean density, and the touch footprint feels proportionate on a tablet.</done>
</task>

</tasks>

<threat_model>
Local untrusted touch-event count and ordering can overflow fixed-size Metal uniform buffers or inject stale forces if not bounded and zeroed. Cap writes to allocated slots, preserve per-touch identity, and explicitly clear unused slots after ends/cancellations. Threat T-02-01 (high): out-of-bounds raw `memcpy` on >5 touches — remove raw allocation/copy and verify capacities. No remote boundary, persisted user content or credentials.
</threat_model>

<verification>
- [ ] iOS and Mac builds succeed after Swift/Metal uniform-capacity changes.
- [ ] No controller raw touch copy or global clear on one finger's end/cancellation remains; check Swift/Metal contact counts and offset alignment.
- [ ] Runtime simulator checks for dye and gestures, including any feasible two-finger gestures, occur in Plan 03; mark unreachable physical multi-touch behavior as unverified rather than passing it based on code alone.
</verification>

<success_criteria>
- Multi-finger iPhone/iPad input remains independent, drag is prompt, single stationary touches add density, and known double-tap shortcuts do not paint; Mac defaults and shortcuts remain usable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modern-ios-parity/02-02-SUMMARY.md` with build results and any interaction observations actually performed.
</output>
