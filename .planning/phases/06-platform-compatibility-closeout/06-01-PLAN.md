---
phase: 06-platform-compatibility-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - AGENTS.md
  - .planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md
autonomous: false
requirements:
  - VER-02
must_haves:
  truths:
    - "VER-02: A developer can build both existing schemes without deployment-target or Swift-version overrides, then follow separate Mac, iPhone and iPad steps for default rendering, dragging, pause/resume, named field selection and the existing four tuning sliders."
    - "Builds, automated tests, process launches and human visual observations are recorded separately with actual host/OS/Xcode or simulator versions; no unrun check is marked PASS."
    - "The macOS 26 runtime launch/drag check remains pending at milestone closeout until observed on a qualifying host; physical-device-only multitouch remains NOT TESTED and non-blocking without a device."
    - "No resolution, in-app reset or new accessibility capability is implemented or promised in this milestone."
  artifacts:
    - README.md
    - AGENTS.md
    - .planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md
  key_links:
    - "README.md build/interaction instructions use the exact app schemes and HUD behavior in the project and controllers, and the completed 06-COMPATIBILITY-VERIFICATION.md cites the commands and observed results."
    - "The Phase 1 macOS 26 follow-up links to 01-BASELINE.md and 01-HUMAN-UAT.md without treating a macOS 27 build or launch as a macOS 26 runtime pass."
---

<objective>
As a developer, I can build and exercise the existing fluid apps on the supported Mac, iPhone and iPad baselines using accurate instructions and an honest record of what was actually verified.

Purpose: Complete the compatibility-verification/documentation requirement without expanding the already-delivered apps.
Output: Updated README and repository notes, plus a dated Mac/iPhone/iPad compatibility-verification record.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
@/Users/abiola/.config/opencode/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-modern-mac-baseline/01-BASELINE.md
@.planning/phases/01-modern-mac-baseline/01-HUMAN-UAT.md
@.planning/phases/02-modern-ios-parity/02-PARITY.md
@.planning/phases/02-modern-ios-parity/02-HUMAN-UAT.md
@.planning/phases/05-live-fluid-tuning/05-TUNING-VERIFICATION.md
@.planning/phases/05-live-fluid-tuning/05-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish a runnable compatibility and interaction guide</name>
  <files>README.md, AGENTS.md, .planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md</files>
  <read_first>README.md, AGENTS.md, build-macos.sh, FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetaliOS/RenderViewController.swift, Shared/SimulationState.swift, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/01-modern-mac-baseline/01-HUMAN-UAT.md, .planning/phases/02-modern-ios-parity/02-PARITY.md, .planning/phases/05-live-fluid-tuning/05-TUNING-VERIFICATION.md, .planning/phases/05-live-fluid-tuning/05-REVIEW.md</read_first>
  <action>
    Update `README.md` to identify the supported macOS 26 / Apple Silicon and iOS/iPadOS 26 targets, Swift 5 project, exact schemes `FluidDynamicsMetalOSX` and `FluidDynamicsMetaliOS`, Metal Toolchain prerequisite, and build commands `xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build` and `xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build`.
    Document `./build-macos.sh` for a launchable root app and Xcode's iOS scheme/run destination on an available iOS 26 iPhone or iPad simulator (derive device IDs with `xcrun simctl list devices available` rather than copying stale IDs).
    Supply a numbered, repeatable checklist for each platform: fresh-launch blue-on-black density and closed Tuning at Force 50%, Dye 40%, Swirl 20%, Fade 17%; drag outside HUD to stir/deposit; on-screen Pause/Resume and Density/Pressure/Velocity/Vorticity selection including a field change while paused; open Tuning, vary one slider and observe its existing effect, then relaunch for defaults.
    Mention Mac Space/S and iOS one-finger/two-finger double taps, with two-finger observations explicitly device-only where simulator tooling lacks them. Replace the stale README claim that all parameters require shader edits: the four sliders are live, but quality/resolution and visible reset are deferred.
    Update `AGENTS.md` stale Swift 4, old deployment-target override and 'no test target/no settings UI' claims to match current Swift 5/macOS 26/iOS 26 settings, state/HUD test targets, native tuning and current verification commands.
    Create `06-COMPATIBILITY-VERIFICATION.md` as a dated checklist/matrix with columns for check, command or human procedure, host/OS/Xcode or simulator, result (PASS/FAIL/NOT TESTED) and evidence; initialize checks NOT TESTED until executed, with separate Mac/iPhone/iPad live observations, automated results and references to Phase 1 macOS 26 and device-only iOS follow-ups.
  </action>
  <verify>Run `git diff --check`; verify README.md contains both scheme names, both no-override build commands, separate platform procedures, all four field names and all four slider names; verify AGENTS.md no longer claims Swift 4 or no test target.</verify>
  <acceptance_criteria>
    - `README.md` shows the exact two Xcode schemes, macOS arm64 and generic iOS Simulator Debug build commands with `CODE_SIGNING_ALLOWED=NO`, and requires no `MACOSX_DEPLOYMENT_TARGET` or `SWIFT_VERSION` override.
    - `README.md` gives observable default, drag, pause/resume, field and Force/Dye/Swirl/Fade steps for Mac, iPhone and iPad, while clearly stating which shortcut requires a physical iOS device to verify.
    - `AGENTS.md` describes actual Swift 5/macOS 26/iOS 26 targets and existing Mac state/HUD and iOS HUD test targets without contradicting current source or the deferred-feature scope.
    - `06-COMPATIBILITY-VERIFICATION.md` has blank or NOT TESTED outcome rows before the checks run; a previously approved Phase 5 observation is attributed as historical evidence, not presented as a fresh Phase 6 run.
  </acceptance_criteria>
  <done>A developer can follow the published build and interaction steps without being sent to obsolete Swift 4 or shader-only tuning instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Execute available build and automated checks and record evidence</name>
  <files>.planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md</files>
  <read_first>.planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md, README.md, AGENTS.md, FluidDynamicsMetal.xcodeproj/project.pbxproj, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/01-modern-mac-baseline/01-HUMAN-UAT.md, .planning/phases/05-live-fluid-tuning/05-TUNING-VERIFICATION.md, .planning/phases/05-live-fluid-tuning/05-REVIEW.md, FluidDynamicsMetalStateTests/SimulationStateTests.swift, FluidDynamicsMetalOSXUITests/HUDUITests.swift, FluidDynamicsMetaliOSUITests/HUDUITests.swift</read_first>
  <action>
    Record `sw_vers -productVersion`, `xcodebuild -version`, actual hardware architecture, and `xcrun simctl list devices available`.
    On the available Apple Silicon host run the README's Mac build command, `./build-macos.sh`, `xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`, and `python3 -m unittest test_phase05_metal.py` after the fresh Mac build.
    Run the generic iOS Simulator Debug build; choose available iOS 26 iPhone and iPad destinations from `xcrun simctl list devices available`, then run `xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,id=<chosen-id>' CODE_SIGNING_ALLOWED=NO` for each feasible device class.
    If an iPad UI test times out, record the timeout and keep it NOT TESTED, distinguishing it from an install/launch and from the prior user-approved iPad observation.
    Check produced Mac bundle is arm64, reports minimum OS 26.0, and contains `default.metallib`; check iOS simulator app's Info.plist minimum OS and metallib when locating its actual build product.
    Populate the verification matrix with exact executed commands and PASS/FAIL/NOT TESTED, test counts and artifact references where available. Do not copy Phase 1 macOS 27 runtime observations into macOS 26 PASS: link its pending `01-HUMAN-UAT.md` / `01-BASELINE.md` closeout check and record any actual macOS 26 run only if an eligible host exists.
    Keep physical two-finger/concurrent-touch checks NOT TESTED without a device, as already approved. If a mandatory build fails, document the error and route to a focused compatibility fix rather than reporting completion.
  </action>
  <verify>Inspect actual `xcodebuild` results, app bundles and `06-COMPATIBILITY-VERIFICATION.md` to confirm every PASS has a matching command or observed action, and every unavailable test is marked NOT TESTED.</verify>
  <acceptance_criteria>
    - Mac and iOS build outcome rows cite commands actually run without deployment-target or Swift-version overrides, and successful Mac bundle checks show arm64, minimum macOS 26.0 and `default.metallib`.
    - Automated state/HUD/GPU results and iPhone/iPad simulator UI results have separate host-specific outcomes; a timed-out iPad run is not a PASS.
    - Mac macOS 27 evidence does not satisfy macOS 26 runtime; `01-HUMAN-UAT.md` stays pending until a real eligible macOS 26 observation is recorded.
    - An unrun manual observation remains NOT TESTED even when its build, launch or automated checks pass.
  </acceptance_criteria>
  <done>Fresh available-host compatibility evidence is recorded without overstating unsupported OS or device coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the published manual Mac, iPhone and iPad walkthrough</name>
  <files>.planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md</files>
  <read_first>README.md, .planning/phases/06-platform-compatibility-closeout/06-COMPATIBILITY-VERIFICATION.md, .planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/01-modern-mac-baseline/01-HUMAN-UAT.md, .planning/phases/05-live-fluid-tuning/05-TUNING-VERIFICATION.md</read_first>
  <action>Ask the user to perform the exact README manual checklist on the available Mac and iOS 26 iPhone/iPad simulator or supported hardware: observe default density/look and slider percentages; drag outside HUD; pause/resume and select all four fields (including while paused); alter a tuning slider and relaunch for defaults. Record their per-platform reported observations, OS/device/version, date and any failures in `06-COMPATIBILITY-VERIFICATION.md`, separate from automated checks and earlier Phase 5 approvals. If a target is unavailable, record NOT TESTED with reason and preserve the Phase 1 macOS 26 runtime check for milestone closeout. Do not block on unavailable physical-only multitouch or fabricate a physical-device result. A reported failure in a mandatory existing interaction triggers a focused compatibility fix and repeat check before Phase 6 sign-off.</action>
  <what-built>Updated developer instructions and actual available-host build, bundle and automated test evidence for both existing apps.</what-built>
  <how-to-verify>Follow README.md on the Mac, an iOS 26 iPhone and an iOS 26 iPad if available. Report which platform and OS you actually used and whether default rendering, drag, pause/resume, all fields, tuning and relaunch matched the checklist; type "approved" with observations or describe a failing or unavailable step.</how-to-verify>
  <verify>Each live-observation row in `06-COMPATIBILITY-VERIFICATION.md` names its platform and source of evidence or remains NOT TESTED with reason; a build-only PASS is never a fluid-interaction PASS.</verify>
  <acceptance_criteria>
    - A Mac, iPhone and iPad each have separately attributed manual outcomes for default rendering, dragging, pause/resume, four named fields and existing tuning or an honest NOT TESTED entry.
    - macOS 26 launch/drag remains a Phase 1 milestone-closeout check unless an actual macOS 26 Metal-capable host was used; iOS physical multitouch remains non-blocking without hardware.
    - Any failure in required current-platform compatibility or interaction has reproduction steps and is not marked complete.
  </acceptance_criteria>
  <done>VER-02 has executable documentation and independently attributed build/test and manual verification evidence for the existing apps.</done>
  <resume-signal>Type "approved" with platform/OS observations, or list the failing and NOT TESTED steps.</resume-signal>
</task>

</tasks>

<threat_model>
No external data or network service is added. The main integrity risk is falsely claiming macOS 26 or physical-device runtime coverage from a macOS 27 build or simulator launch. Host-tagged PASS/FAIL/NOT TESTED results, a separate human observation checkpoint and the retained Phase 1 closeout check prevent that error. No high-severity external threat identified.
</threat_model>

<verification>
- [ ] `git diff --check` passes; README and AGENTS commands agree with the actual schemes, build settings and controls.
- [ ] Both schemes' no-override build commands and available automated suites have freshly recorded outcomes with real host and simulator versions.
- [ ] Manual Mac/iPhone/iPad checklist results are user-observed or explicitly NOT TESTED; macOS 26 and physical multitouch claims are not inferred from other hosts.
- [ ] VER-02 is covered; no CTRL-05/06/07 or new solver/UI work is included.
</verification>

<success_criteria>
- Developers have reproducible build and manual verification instructions for the existing macOS 26 / Apple Silicon and iOS/iPadOS 26 apps (VER-02), with truthful available-host evidence and the outstanding macOS 26 runtime check carried into milestone closeout.
</success_criteria>

<output>
After the checkpoint, create `.planning/phases/06-platform-compatibility-closeout/06-01-SUMMARY.md` with command outcomes, attributed manual observations and remaining NOT TESTED items; update the roadmap/state through GSD handlers during phase execution.
</output>
