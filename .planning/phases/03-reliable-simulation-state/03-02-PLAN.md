---
phase: 03-reliable-simulation-state
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Shared/Renderer.swift
  - Shared/Shaders.metal
  - FluidDynamicsMetaliOS/RenderViewController.swift
  - FluidDynamicsMetalOSX/RenderViewController.swift
  - test_phase03_metal.swift
  - test_phase03_metal.py
autonomous: true
requirements:
  - SIM-02
  - SIM-03
must_haves:
  truths:
    - "D-01: Mac live resize or iOS rotation/view resize preserves density, pressure, velocity and vorticity patterns across the entire new canvas, including drastic aspect changes."
    - "D-02: A drag crossing resize uses fresh view-local coordinates without an old-position impulse; a new touch/mouse drag works immediately at the new size."
    - "D-03, D-04: Resize during user pause presents the fitted selected field without simulation advancement and resume continues the existing fluid and field."
    - "D-05, D-08: Input and taps held over resize/inactivity never strand a force; a fresh active input is accepted."
  artifacts:
    - Shared/Shaders.metal
    - Shared/Renderer.swift
    - test_phase03_metal.swift
    - test_phase03_metal.py
  key_links:
    - "Renderer.mtkView(_:drawableSizeWillChange:) retains old Slab ping textures until a Metal migration pass populates all five new ping textures."
    - "The new `.rg16Float` resample fragment entry point is bound by a matching RenderShader name and compiled into both app metallibs."
    - "The paused draw-only path from Plan 01 displays the preserved selection after resize; controllers rebase pointer/touch positions for the replacement grid."
---

<objective>
As a fluid user, I want my existing pattern to fit a resized window or rotated screen so I can keep stirring without losing fluid or receiving an input jump.

Purpose: Preserve the entire GPU simulation through drawable changes and maintain pointer/touch continuity, including while paused.
Output: An offscreen Metal resampling contract, safe five-slab migration in `Renderer`, and input rebasing on Mac and iOS.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-reliable-simulation-state/03-CONTEXT.md
@.planning/phases/03-reliable-simulation-state/03-RESEARCH.md
@.planning/phases/03-reliable-simulation-state/03-VALIDATION.md
@.planning/phases/03-reliable-simulation-state/03-PATTERNS.md
@.planning/phases/03-reliable-simulation-state/03-01-SUMMARY.md
@Shared/Renderer.swift
@Shared/Slab.swift
@Shared/Shaders.metal
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve a full fluid texture across aspect changes with a real GPU check</name>
  <files>Shared/Shaders.metal, Shared/Renderer.swift, test_phase03_metal.swift, test_phase03_metal.py</files>
  <read_first>Shared/Shaders.metal, Shared/Renderer.swift, Shared/Slab.swift, Shared/RenderShader.swift, Shared/MetalDevice.swift, test_phase02_metal.swift, test_phase02_metal.py, test_phase03_metal.swift (create if absent), test_phase03_metal.py (create if absent), .planning/phases/03-reliable-simulation-state/03-CONTEXT.md, .planning/phases/03-reliable-simulation-state/03-RESEARCH.md</read_first>
  <action>Add one new fragment shader entry point `resampleField` returning unmodified RG components sampled from texture(0) with normalized UV, a clamped sampler and a small optional uniform providing velocity x/y factors; pass `(newWidth/oldWidth, newHeight/oldHeight)` for velocity, `(1,1)` for the other four fields. Use the existing `vertexShader` and `.rg16Float` `RenderShader` full-screen indexed pass. In `Renderer.mtkView(_:drawableSizeWillChange:)`, reject invalid/zero logical view dimensions, skip an unchanged size, retain prior five slabs and 3-buffer state, and allocate replacement slabs at the new logical grid size. When old slabs exist, encode five old-ping → new-ping resample passes for velocity, density, divergence, vorticity and pressure on `MetalDevice.sharedInstance` serial command queue; do not copy old pong (it is next overwritten), do not sample a texture while writing into it, and do not use `visualizeScalar` which transforms values. Coordinate against up to `Renderer.MaxBuffers` in-flight draw commands before replacing uniform buffers/slab references: drain the three semaphore permits without deadlocking completion, commit/wait for the offscreen migration (check `.status`/`.error`), then publish replacements and new `StaticData.offsets`/`screenSize`, releasing permits on every exit path. Keep old fields intact if migration fails. On initial allocation there are no old slabs to copy. Preserve field/pause state from Plan 01, and call a presentation-only `view.draw()` if the resize occurs while paused and a drawable is available; do not advance the solver. Build the Mac app, then create `test_phase03_metal.swift` modeled on `test_phase02_metal.swift` and `test_phase03_metal.py` modeled on `test_phase02_metal.py`: load the **fresh Mac built metallib**, seed distinct interior + edge RG16F values in an offscreen 64×32 field, resample to 32×96, assert all old corners and center survive at their normalized new locations to within RG16F sampling tolerance, verify the velocity x/y scale factors, and verify pressure/density use factors 1. Require the Python unittest to fail on a missing shader, a blank destination, cropping, or incorrect component scaling.</action>
  <verify><automated>python3 -m unittest -v test_phase03_metal</automated></verify>
  <acceptance_criteria>
    - The Mac build succeeds with `resampleField` and the unittest passes against its actual `default.metallib`; readback assertions demonstrate density, pressure and velocity preservation across 64×32→32×96 without black edge bands or cropping.
    - Renderer migration copies all five old `ping` fields before publishing new slabs; `pong` remains available for later solver writes, and `initBuffers` reflects the new nonzero grid dimensions.
    - An unchanged or zero-sized callback does not allocate slabs, cause a division by zero, or replace the selected field; migration failure does not silently discard old fluid.
    - The active simulation, selected field and manual pause state remain unchanged by migration; a paused resize presents the preserved fluid without any solver pass.
  </acceptance_criteria>
  <done>All selected GPU fields survive a major aspect resize with measured offscreen texture readback.</done>
</task>

<task type="auto">
  <name>Task 2: Rebase held Mac and iOS input at the resized canvas</name>
  <files>Shared/Renderer.swift, FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetalOSX/RenderViewController.swift</files>
  <read_first>Shared/Renderer.swift, Shared/SimulationState.swift, Shared/Slab.swift, FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetalOSX/RenderViewController.swift, Shared/Shaders.metal, test_phase03_metal.swift, .planning/phases/03-reliable-simulation-state/03-CONTEXT.md, .planning/phases/03-reliable-simulation-state/03-VALIDATION.md</read_first>
  <action>Keep existing logical view-point input conventions (`screenSize`, mouse/touch position, `inkRadius`) when changing grid dimensions; use `view.bounds` and the resized view's coordinates consistently rather than mixing a pixel `drawableSize` with point-space touches. On a valid resize, invalidate old renderer `directions` and queued taps/contacts; seed the next mouse-drag prior position with its post-resize view-local coordinate so its impulse begins at zero. Ensure Mac uses `metalView.convert(event.locationInWindow, from: nil)` for both mouse-down and drag and accounts for a nonzero view origin. On iOS, rebase every tracked active `UITouch` through `touch.location(in: metalView)` after layout/rotation, update `positions`, `previousPositions`, `startPositions` consistently, cancel stale pending hold jobs as needed, and submit zero-impulse or no contact until the next real movement. A bounds/layout callback (`viewDidLayoutSubviews`) can handle iOS view changes in addition to the renderer's drawable-size callback; avoid dropping the active touch identity or blindly retaining coordinates from before rotation. After resize, touch/mouse input must stir promptly, and a paused resize must remain paused while reflecting the new aspect. Build the iOS and Mac Debug schemes, rerun state tests and the offscreen resample check; record which live-resize/rotation behaviors still need Plan 03 observation.</action>
  <verify><automated>xcodebuild -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetaliOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.4' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <acceptance_criteria>
    - Both no-override Debug app builds succeed; `xcodebuild test` state suite and `python3 -m unittest -v test_phase03_metal` still exit 0 after controller integration.
    - Mac mouse-down and drag use `metalView.convert(event.locationInWindow, from: nil)`; resize resets the prior impulse so the first continued movement has no pre-resize delta.
    - iOS active touch history is rebased from the new `metalView` layout and delayed holds/taps do not inject a stale pre-rotation force; a newly begun touch stirs after return.
    - `MTKView.isPaused` and selected field remain unchanged by a resize; paused redraw follows the draw-only branch and does not advect/fade existing fluid.
  </acceptance_criteria>
  <done>A resize changes the canvas, not the user's selected field, pause choice or interaction continuity.</done>
</task>

</tasks>

<threat_model>
Texture dimensions and pointer coordinates derive from local window/device layout, not untrusted remote input. Validate zero/invalid dimensions, avoid stale GPU resource use while in-flight buffers complete, and avoid out-of-range contact impulses. High-severity external threats: none identified; GPU state loss/deadlock is a correctness risk checked by repeated live resizing and offscreen shader assertions.
</threat_model>

<verification>
- [ ] Offscreen GPU test uses the built `resampleField` shader and confirms full-canvas normalized resampling and per-component velocity scaling.
- [ ] XCTest and Mac/iOS Debug builds pass after `Renderer` and both controllers are changed.
- [ ] Manual Mac live resize and iOS rotation/resize (including pause and held input) are observed in Plan 03.
</verification>

<success_criteria>
- Existing fluid and selected field survive dramatic resize/rotation on both apps, paused rendering stays frozen, and stirring remains responsive without a stale-contact jump (SIM-02, SIM-03).
</success_criteria>

<output>
Create `.planning/phases/03-reliable-simulation-state/03-02-SUMMARY.md` with exact build/test results, shader readback values/tolerances, resize-synchronization behavior and unverified UI cases.
</output>
