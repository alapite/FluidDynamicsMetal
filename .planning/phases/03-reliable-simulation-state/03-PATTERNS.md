# Phase 3 — Existing code patterns

| Proposed file | Role / data flow | Closest existing analog and concrete rule |
|---------------|------------------|------------------------------------------|
| `Shared/SimulationState.swift` | Shared CPU-only selected field, pause/lifecycle and tuning state | `Shared/Renderer.swift` lines 89–104 (`currentIndex`, `nextSlab`) and Phase 1 `01-BASELINE.md` lines 25–36 for defaults. Add to both PBXSourcesBuildPhase entries in `FluidDynamicsMetal.xcodeproj/project.pbxproj` (lines 240–269). |
| `Shared/Renderer.swift` | State → contacts/uniforms → simulation passes → displayed slab | `draw(in:)` lines 309–379 already uses triple uniform buffers and a semaphore; `drawSlab()` lines 186–197 maps field index to slabs. `initSurfaces`/`initBuffers` allocate all five fields. Preserve Swift/Metal uniform order. |
| `Shared/Shaders.metal` | Old texture ping → replacement ping on resize | `visualizeScalar`/`visualizeVector` lines 32–46 demonstrate normalized UV sampling; add a separate RG16F-preserving fragment pass, not a visualization shader. `RenderShader.calculateWithCommandBuffer` encodes the full-screen indexed quad (lines 54–72). |
| `Shared/Slab.swift` | Ping/pong GPU texture owner | Lines 16–28 make `.shaderRead | .renderTarget` textures; lines 30–34 swap after each solver write. Copy old ping into new ping and let the first later solver pass replace pong. |
| `FluidDynamicsMetaliOS/RenderViewController.swift` | Touch identity/gestures/app lifecycle → renderer | Lines 70–135 maintain positions, previous positions, delayed holds and active touches; lines 138–164 handle shortcuts/active notifications. Cancel delayed holds, reset prior touch positions and explicitly clear renderer contacts when pausing/resigning active. |
| `FluidDynamicsMetalOSX/RenderViewController.swift` | Mouse and Space/S → renderer | Lines 36–74 use window-position conversions and toggle `metalView.isPaused`; route through renderer state and use view-local point conversion for resize. |
| `FluidDynamicsMetal.xcodeproj/project.pbxproj`, `FluidDynamicsMetalStateTests/SimulationStateTests.swift` | Test target/scheme → production shared state | No existing test target; existing `test_phase02_metal.py` uses Python unittest and freshly built Mac `default.metallib` for GPU integration. State tests should compile the production shared file; confirm scheme TestAction via actual `xcodebuild test`. |
| `test_phase03_metal.swift`, `test_phase03_metal.py` | Nonuniform GPU resize contract | `test_phase02_metal.swift` seeds/readbacks `.rg16Float` textures and actual shader entry points; `test_phase02_metal.py` locates the Mac built metallib via `xcodebuild -showBuildSettings -json`. Reuse this method, with different source/destination dimensions and edge/center assertions. |

Check `.planning/phases/03-reliable-simulation-state/03-CONTEXT.md` D-01–D-11 for the locked behavior, especially paused rendering, lifecycle intent, and fitted full-image resize.
