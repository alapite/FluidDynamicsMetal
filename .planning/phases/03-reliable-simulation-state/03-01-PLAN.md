---
phase: 03-reliable-simulation-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/SimulationState.swift
  - Shared/Renderer.swift
  - FluidDynamicsMetaliOS/RenderViewController.swift
  - FluidDynamicsMetalOSX/RenderViewController.swift
  - FluidDynamicsMetal.xcodeproj/project.pbxproj
  - FluidDynamicsMetal.xcodeproj/xcshareddata/xcschemes/FluidDynamicsMetalOSX.xcscheme
  - FluidDynamicsMetalStateTests/SimulationStateTests.swift
autonomous: true
requirements:
  - SIM-03
  - VER-01
must_haves:
  truths:
    - "D-04, D-09: Field order is density → pressure → velocity → vorticity → density, and a paused field switch is presented immediately without advancing the simulation."
    - "D-03, D-04, D-10: Manual pause survives a field change and subsequent resize; resume retains the chosen field and continues from the frozen state."
    - "D-05: Paused mouse/touch/tap input does not queue dye or force, and a held drag resumes from its current location without a stale-position impulse."
    - "D-06, D-07, D-08: iOS inactivity suspends drawing while remembering the user's pause choice and field, clears interrupted touches and pending dye, and returns without a catch-up solver step."
    - "D-11: Executable tests cover bounded tuning defaults and out-of-range inputs and resetting mutated state to defaults; no new visible controls."
  artifacts:
    - Shared/SimulationState.swift
    - FluidDynamicsMetalStateTests/SimulationStateTests.swift
    - FluidDynamicsMetal.xcodeproj/xcshareddata/xcschemes/FluidDynamicsMetalOSX.xcscheme
  key_links:
    - "Both app targets and the state test target compile the same Shared/SimulationState.swift; the Mac scheme TestAction runs its XCTest bundle."
    - "Controller shortcuts call Renderer pause/field APIs; Renderer.draw(in:) branches on state.shouldAdvance before contacts, uniforms or solver passes, but still visualizes the selected slab."
    - "The iOS lifecycle notifications suspend effective drawing without overwriting userPaused; both controller input paths rebase held contact history."
---

<objective>
As a fluid user, I want to pause, inspect another field and return to the same responsive simulation so I can explore it without losing my place.

Purpose: Establish the shared state contract, a real state test suite and an end-to-end paused-render/lifecycle slice on both apps before GPU resize migration.
Output: Shared state, runnable macOS XCTest target, paused render-only path, existing shortcut integration and iOS activity preservation.
</objective>

<execution_context>
@/Users/abiola/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/abiola/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-reliable-simulation-state/03-CONTEXT.md
@.planning/phases/03-reliable-simulation-state/03-RESEARCH.md
@.planning/phases/03-reliable-simulation-state/03-VALIDATION.md
@.planning/phases/03-reliable-simulation-state/03-PATTERNS.md
@.planning/phases/02-modern-ios-parity/02-CONTEXT.md
@.planning/phases/01-modern-mac-baseline/01-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production state through a macOS unit test target</name>
  <files>Shared/SimulationState.swift, FluidDynamicsMetalStateTests/SimulationStateTests.swift, FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetal.xcodeproj/xcshareddata/xcschemes/FluidDynamicsMetalOSX.xcscheme</files>
  <read_first>Shared/SimulationState.swift (create if absent), Shared/Renderer.swift, Shared/Shaders.metal, FluidDynamicsMetal.xcodeproj/project.pbxproj, FluidDynamicsMetal.xcodeproj/xcshareddata/xcschemes/FluidDynamicsMetalOSX.xcscheme (create if absent), .planning/phases/01-modern-mac-baseline/01-BASELINE.md, .planning/phases/03-reliable-simulation-state/03-CONTEXT.md, .planning/phases/03-reliable-simulation-state/03-RESEARCH.md</read_first>
  <action>Add a Foundation-only `SimulationState` used as the one source of truth for an ordered `DisplayField` (`density=0`, `pressure=1`, `velocity=2`, `vorticity=3`), `userPaused`, `inactive`, computed `shouldAdvance`, `nextField()`, `togglePause()`, `resignActive()`, `becomeActive()` and `resetTuning()`. Add a `SimulationTuning` value type with defaults `force=1` (multiplier of the existing per-contact impulse), `dye=0.8`, `radius=150`, `swirl=0.4`, `retention=0.998`, `pressureIterations=40`; bound force/dye/swirl at `0...2`, radius at `1...400`, retention at `0...1`, iterations at `1...80`, clamp finite overflow, and substitute the per-property default for NaN/infinity. This phase records and tests tuning state; it does not change shader constants or wire tuning into live solver controls. Write XCTest tests that drive this **production file**, asserting all four field steps and wrap, running→paused→field switch→resumed, running and user-paused inactivity/return, each bound (low/high) and non-finite fallback, mutation and state-level reset (tuning defaults; resetting tuning does not unintentionally change selected field or pause). Register the shared source with both app targets, add a macOS XCTest bundle target compiling the same file, configure test host/bundle settings appropriately (logic tests without app host if possible), and add a shared Mac scheme TestAction referencing the test bundle while retaining BuildAction/LaunchAction for the app. Run the exact `xcodebuild test` command below and correct project wiring until XCTest methods actually execute, not merely `BUILD SUCCEEDED`.</action>
  <verify><automated>xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO</automated></verify>
  <acceptance_criteria>
    - `SimulationStateTests.swift` tests density→pressure→velocity→vorticity→density, preserved selection through pause/resume/inactive return, every tuning property at both limits, non-finite inputs and reset after non-default values.
    - `project.pbxproj` lists `Shared/SimulationState.swift` in both app Sources and the test target Sources; Mac shared scheme has a TestAction for the unit-test bundle.
    - The specified `xcodebuild test` exits 0 and reports executed tests (not only a build); initial display remains density and baseline default values are asserted.
  </acceptance_criteria>
  <done>Production shared state is executable, bounded, resettable and covered by a runnable Xcode test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Keep paused field and iOS return responsive without solver advancement</name>
  <files>Shared/Renderer.swift, FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetalStateTests/SimulationStateTests.swift</files>
  <read_first>Shared/Renderer.swift, Shared/SimulationState.swift, Shared/Shaders.metal, FluidDynamicsMetaliOS/RenderViewController.swift, FluidDynamicsMetalOSX/RenderViewController.swift, FluidDynamicsMetalStateTests/SimulationStateTests.swift, .planning/phases/03-reliable-simulation-state/03-CONTEXT.md, .planning/phases/03-reliable-simulation-state/03-VALIDATION.md</read_first>
  <action>Give `Renderer` a `SimulationState` instance and expose small controller-facing pause, field-switch, activity and clear-input methods. Replace its private `currentIndex` with the ordered state field in `drawSlab()` and render-pipeline selection; on a paused field switch call `metalView.draw()` to present the new slab immediately, without entering advection, force, vorticity, divergence, 40-pressure or gradient steps. Gate `draw(in:)` at the beginning on `state.shouldAdvance`: the paused branch only encodes rendering/presentation (and releases the semaphore) and clears/rejects queued contacts/taps, while the running branch preserves the existing solver sequence. Keep `MTKView.isPaused` synchronized with effective state and use explicit paused redraw for field changes. On Mac route Space and S through these APIs; ignore input while paused and rebase the first movement on resume using the current view-local point from `metalView.convert(event.locationInWindow, from: nil)` and an active-mouse flag instead of reusing a pre-pause position. On iOS retain one-finger pause and two-finger field double taps; on pause and `willResignActive`, cancel pending holds and clear `touchOrder`, `positions`, `previousPositions`, `startPositions`, `activeTouches` and renderer contacts/pending taps. On `didBecomeActive`, remove only the lifecycle pause, leaving the user pause intact; block single-tap dye and touch submission while inactive/paused. For a touch still held across a manual pause, either discard its pre-pause history until a new begin or seed the next active move's prior position from its current location; do not generate a force spike. Add state tests for effective pause and field retention; compare the existing baseline shader values and shortcut selectors after editing. Compile both schemes after the XCTest run.</action>
  <verify><automated>xcodebuild test -project FluidDynamicsMetal.xcodeproj -scheme FluidDynamicsMetalOSX -configuration Debug -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO</automated></verify>
  <acceptance_criteria>
    - `Renderer.draw(in:)` can render the selected slab while `state.shouldAdvance == false` without invoking any solver pass; pending taps and contacts cannot be replayed on resume.
    - `Renderer.nextSlab()` or its replacement presents a new field when paused; both controllers retain Space/S and the iOS double-tap shortcuts, and preserve the selected field after pause/resume.
    - iOS notification handlers preserve manual pause choice, clear interrupted touch and delayed hold state, and a later new touch can stir; no elapsed background time feeds extra solver iterations.
    - Mac pointer positions are local to `metalView`, and a held drag resumed after pause starts with no old-coordinate impulse; Xcode state tests execute and both schemes build.
  </acceptance_criteria>
  <done>Paused field inspection and brief iOS inactivity are consistent on both platforms; solver remains frozen until actual resume.</done>
</task>

</tasks>

<threat_model>
This phase reads only local touch/mouse events and GPU textures; no network, account or persisted input is introduced. The main robustness risks are non-finite/out-of-range values and stale touch history entering GPU uniforms; the bounded state contract and paused/inactive input clearing address them. High-severity external threats: none identified.
</threat_model>

<verification>
- [ ] `xcodebuild test` executes the new production-state XCTest suite and reports passing state/parameter assertions.
- [ ] Mac and iOS Debug schemes compile with the shared state source and current Metal layout.
- [ ] Pause, field switch while paused, resume and iOS inactivity paths retain user intent and clear interrupted input; observational sign-off is in Plan 03.
</verification>

<success_criteria>
- A frozen fluid can be inspected in any field; resume keeps its field and does not replay input. Running/paused iOS choices survive inactivity and repeatable state tests establish default/bounds/reset behavior (SIM-03, VER-01).
</success_criteria>

<output>
Create `.planning/phases/03-reliable-simulation-state/03-01-SUMMARY.md` recording the actual test count, both build results and any UI observations versus still-manual cases.
</output>
